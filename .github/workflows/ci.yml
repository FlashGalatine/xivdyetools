name: CI

on:
  # Pushes: the protected lines only -- main, plus the long-lived integration
  # branches that feed it (`*-prep`, e.g. monorepo-2.0-prep for the 5.0
  # wave). Feature branches are covered by their pull request instead; a push
  # trigger for every branch would run the same commits twice, and the
  # push-side `...[HEAD^]` filter only diffs the last commit anyway.
  push:
    branches: [main, master, '*-prep']
  # Pull requests: every PR, whatever its base. Until 2026-08-22 this was
  # `branches: [main, master]`, so PRs into monorepo-2.0-prep had no CI at
  # all and `gh pr merge` would merge red (PR #130 was merged on local runs).
  # On a PR event HEAD is the merge ref and HEAD^ the base tip, so the
  # `...[HEAD^]` filters below scope the run to what the PR changed.
  pull_request:
  # Advisories are published continuously, but this workflow only ran when
  # somebody pushed -- so one landing during a quiet stretch went unseen until
  # the next PR. The nightly run closes that gap. Only the `audit` job takes
  # part (see the `if` on `ci` below).
  #
  # Daily, not hourly: `minimumReleaseAge: 1440` in pnpm-workspace.yaml forbids
  # installing a patch published in the last 24h, so detecting faster than
  # daily surfaces nothing that can be acted on yet.
  #
  # 11:17 UTC ~ 07:17 in the maintainer's -0400, so a failure notification is
  # waiting at the start of the day. The offset minute is deliberate:
  # top-of-hour crons queue behind everyone else's and start late.
  #
  # Reminder: GitHub runs schedules only from the DEFAULT branch's copy of this
  # file, so edits here do nothing until they land on main. GitHub also
  # disables scheduled workflows after 60 days without repository activity.
  schedule:
    - cron: '17 11 * * *'

# FINDING-009 (2026-08-21 security audit): least-privilege GITHUB_TOKEN —
# the default repo token is never needed beyond checkout here. Jobs that need
# more (npm OIDC publish) declare it at job level.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ARCH-003: Scan production dependencies for known vulnerabilities.
  #
  # Its own job on purpose. As a step inside `ci` it sat ahead of lint,
  # type-check, test and build, so a newly published advisory against a
  # dependency nobody touched would have stopped all four from running --
  # a broken PR and a clean one would have shown the same red X. It was
  # therefore pinned to continue-on-error, which made a critical advisory
  # (GHSA-mv8w-475r-vwqw, found 2026-08-12) invisible in a green-looking run.
  # Split out, the two signals are independent: this job can fail loudly
  # without costing any test coverage signal.
  #
  # Note for whoever hits this red: `minimumReleaseAge: 1440` in
  # pnpm-workspace.yaml means a patch published in the last 24h cannot be
  # installed, so a same-day advisory may have no permitted fix yet. That is
  # the intended trade -- wait it out, or add a dated minimumReleaseAgeExclude.
  audit:
    name: Security audit (production dependencies)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm audit --prod --audit-level high

  # FINDING-030 (2026-08-21 security audit): secret scanning in CI. On push /
  # pull_request the action scans exactly the commits of the event (the
  # pre-commit detect-secrets config it replaces never ran anywhere). Rules +
  # allowlist live in `.gitleaks.toml` at the repo root. This is a personal-
  # account repository, so no GITLEAKS_LICENSE is required. GitHub's own
  # secret scanning + push protection (repo settings) is the complement that
  # covers full history and blocks pushes.
  secret-scan:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # The action diffs the event's commit range, so it needs history.
          fetch-depth: 0
          persist-credentials: false

      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: ${{ github.workspace }}/.gitleaks.toml
          # 2026-08-31: pin the scanner, not just the action. v3.0.0 installs a
          # hard-coded 8.24.3, which does NOT apply a global `[[allowlists]]`
          # `paths` entry to findings in git/diff mode — so every run since
          # Sprint 4 failed on `BOT_SIGNING_SECRET: 'test-signing-secret-…'` in
          # a `.test.ts` file that `.gitleaks.toml` explicitly exempts. 8.30.1
          # honours it: same 30 commits and same 1,293,509 bytes, 1 leak on
          # 8.24.3 and 0 on 8.30.1, for both `detect` and `git`. The config is
          # written and verified against 8.30.1, so the version it is verified
          # against is the version CI must run. Re-verify the allowlist locally
          # before raising this (see evidence/scripts/05-gitleaks.sh).
          GITLEAKS_VERSION: '8.30.1'
          # contents: read only — PR comments would need pull-requests: write.
          GITLEAKS_ENABLE_COMMENTS: 'false'
          GITLEAKS_ENABLE_SUMMARY: 'true'
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'

  ci:
    name: Lint, Type-check, Test, Build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Every turbo filter below is '...[HEAD^]'. On the nightly schedule that
    # resolves to whatever the last merge happened to touch, so this job would
    # re-run an arbitrary slice of the suite against an unchanged tree. The
    # schedule exists for the audit alone.
    if: github.event_name != 'schedule'
    steps:
      # fetch-depth: 2 is load-bearing -- every turbo filter below is
      # '...[HEAD^]' and needs the previous commit present to diff against.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 2

      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # 2026-08-30 FINDING-023 S8-R14: every step below is filtered by
      # '...[HEAD^]' -- changed packages PLUS THEIR PNPM/TURBO DEPENDENTS.
      # These four wrangler-config tests are the one place in the repo a
      # CROSS-PACKAGE invariant is asserted (each reads at least one
      # sibling app's wrangler.toml), and none of the four apps is a
      # dependent of any other in the pnpm/turbo graph (image-worker
      # depends only on worker-kit) -- so affected-package selection is
      # structurally wrong for them. A PR that edits only, say,
      # discord-worker/wrangler.toml (retargeting the IMAGE_WORKER service
      # binding, or presets-api/wrangler.toml retargeting TOKEN_BLACKLIST)
      # selects that one app alone; the sibling's cross-worker assertion --
      # the only test that would have caught the drift -- never runs, and
      # the PR merges green only to fail loudly at deploy time with nothing
      # here to warn first. Run unconditionally, every CI invocation,
      # regardless of what changed. Cheap (single-digit seconds each) and
      # needs nothing beyond the install above -- no build, no --filter.
      # `--coverage.enabled=false` overrides oauth's vitest.config.ts
      # (`coverage.enabled: true` unconditionally, 90/85/90/90 thresholds),
      # which would otherwise fail this narrow a slice on coverage alone,
      # not on its assertions; applied to all four for one consistent
      # invocation shape even though only oauth needs it today. `set +e` +
      # manual exit-code accumulation so all four run and report even when
      # an earlier one is red, instead of bash's default stop-on-first-
      # failure (verified locally: a mid-list failure still lets the last
      # one run, and the step's own exit code is still non-zero).
      #
      # Do NOT add apps/*/wrangler.toml to any DEPLOY workflow's path
      # filter -- that would make editing one worker's config deploy a
      # different one. This step only ever runs `vitest run` against a
      # named file; it deploys nothing.
      - name: Wrangler config invariants (cross-package, always run)
        run: |
          set +e
          status=0
          pnpm --filter xivdyetools-image-worker exec vitest run src/wrangler-config.test.ts --coverage.enabled=false
          status=$((status + $?))
          pnpm --filter xivdyetools-presets-api exec vitest run tests/wrangler-config.test.ts --coverage.enabled=false
          status=$((status + $?))
          pnpm --filter xivdyetools-moderation-worker exec vitest run tests/wrangler-config.test.ts --coverage.enabled=false
          status=$((status + $?))
          pnpm --filter xivdyetools-oauth-worker exec vitest run src/__tests__/wrangler-config.test.ts --coverage.enabled=false
          status=$((status + $?))
          exit $status

      - name: Lint (affected)
        run: pnpm turbo run lint --filter='...[HEAD^]'

      - name: Type-check (affected)
        run: pnpm turbo run type-check --filter='...[HEAD^]'

      # Repo-wide by nature (not filtered by '...[HEAD^]' like the turbo steps
      # above): the reachability gate reads every tracked TS/JS source file
      # to find modules, exports and class members reachable only from tests --
      # a narrow diff can make a symbol elsewhere test-only, so scoping this to
      # "affected" packages would miss exactly the drift it exists to catch.
      # Costs seconds. Order matters: type-check the scripts, then run the
      # checker's own test suite, then the checker itself -- a broken checker
      # that still exits 0 is worse than no gate at all.
      - name: Type-check (scripts)
        run: pnpm type-check:scripts

      - name: Dead-code checker self-test
        run: pnpm test:scripts

      - name: Dead-code reachability
        run: pnpm dead-code:check

      # --concurrency=3: when a change touches every package (docs sweeps,
      # formatting passes), the affected filter selects all ~34 test tasks and
      # turbo's default concurrency of 10 runs that many vitest processes --
      # each with its own worker pool -- on a 2-core runner. Under that load a
      # vitest child gets silently killed and the task fails with exit 1 and no
      # assertion failure (observed on the 2026-07-28 docs sweep, three runs in
      # a row, @xivdyetools/core#test dying mid-run). Narrow diffs are
      # unaffected; wide sweeps get bounded parallelism instead of an OOM.
      - name: Test (affected)
        run: pnpm turbo run test --filter='...[HEAD^]' --continue --concurrency=3

      - name: Build (affected)
        run: pnpm turbo run build --filter='...[HEAD^]'

      # OPT-002: only discord-worker declares `check-bundle-size`, so this is a
      # no-op on any change that does not affect it. It gates the 3,072 KiB
      # gzipped Worker cap here, on the PR, rather than leaving Cloudflare to
      # refuse the upload after a merge has already triggered the deploy.
      - name: Check worker bundle sizes (affected)
        run: pnpm turbo run check-bundle-size --filter='...[HEAD^]'

      # ARCH-004: Report bundle sizes for built packages
      - name: Report bundle sizes
        if: always()
        run: |
          echo "## 📦 Bundle Sizes" >> $GITHUB_STEP_SUMMARY
          echo "| Package | JS Output | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          for dir in apps/*/dist packages/*/dist; do
            if [ -d "$dir" ]; then
              name=$(echo "$dir" | sed 's|/dist||')
              js_bytes=$(find "$dir" \( -name '*.js' -o -name '*.mjs' \) -exec cat {} + 2>/dev/null | wc -c)
              if [ "$js_bytes" -gt 0 ]; then
                js_human=$(numfmt --to=iec "$js_bytes")
                if [ "$js_bytes" -gt 5242880 ]; then
                  echo "| $name | $js_human | ⚠️ >5 MiB |" >> $GITHUB_STEP_SUMMARY
                else
                  echo "| $name | $js_human | ✅ |" >> $GITHUB_STEP_SUMMARY
                fi
              fi
            fi
          done

  # E2E (Playwright). Its own job so a browser install and a dev server never
  # sit in front of lint/type-check/test, and so a red E2E run is a distinct
  # signal rather than one more way for `ci` to go red.
  #
  # Why this job exists: until 2026-09-03 Playwright ran in NO workflow. The
  # `ci` job's test step is `turbo run test` (vitest only), so the E2E suite
  # was green-by-absence. `ca0ee36d` — an i18n grammar fix that changed the
  # palette drawer's favourites aria-label from "Add to favorites: <name>" to
  # "Add <name> to favorites" — broke two `collection-manager.spec.ts`
  # selectors and merged with every check green; the breakage was only found
  # by running the suite by hand three PRs later.
  #
  # Runs on EVERY invocation, not `--filter='...[HEAD^]'`. Affected-package
  # selection is the wrong tool here for the same reason the wrangler-config
  # step above opts out of it: these specs drive the built app end to end, so a
  # change anywhere in web-app's dependency graph (core's dye data, svg's
  # glyphs, a locale string in any package) can red them, and the failure that
  # motivated the job was exactly a non-obvious cross-package one. ~4 minutes
  # against the cost of shipping a broken UI.
  #
  # chromium ONLY, deliberately. The `mobile-chrome` project has 28 standing
  # failures that are a LIVE PRODUCT BUG, not test rot — at phone width
  # `v4-layout-shell` renders '' in place of `<v4-config-sidebar>`, so every
  # left-panel control is unreachable. `apps/web-app/e2e/COVERAGE-GAPS.md`
  # documents it in full and says explicitly not to "fix" those tests. Adding
  # the project here would make CI permanently red and the fix would be to
  # delete the only thing reporting the gap. Add it in the same PR that lands
  # the shell fix, not before.
  e2e:
    name: E2E (Playwright, chromium)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Same reasoning as `ci`: the nightly schedule exists for the audit alone,
    # and re-running the browser suite against an unchanged tree buys nothing.
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # `playwright.config.ts` starts `npm run dev` (Vite) as its webServer, and
      # Vite resolves `@xivdyetools/*` to each package's `dist`. Without this
      # the dev server boots and every spec fails on an unresolved import.
      # `web-app...` is web-app AND everything it depends on.
      - name: Build web-app and its workspace dependencies
        run: pnpm turbo run build --filter=xivdyetools-web-app...

      # Keyed on the lockfile: the browser build Playwright wants is pinned by
      # the @playwright/test version, which only moves when the lockfile does.
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('pnpm-lock.yaml') }}

      # `--with-deps` installs the system libraries too and is needed even on a
      # cache hit, since those live outside ~/.cache/ms-playwright.
      - name: Install Chromium
        run: pnpm --filter xivdyetools-web-app exec playwright install --with-deps chromium

      - name: Run E2E suite
        run: pnpm --filter xivdyetools-web-app exec playwright test --project=chromium

      # Only on failure: the HTML report is the first thing worth opening when
      # a spec goes red, and it carries the screenshot + trace the config
      # captures (`screenshot: 'only-on-failure'`, `trace: 'on-first-retry'`).
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report
          path: |
            apps/web-app/playwright-report/
            apps/web-app/test-results/
          retention-days: 7
