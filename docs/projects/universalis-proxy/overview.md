# universalis-proxy (merged)

> Merged into [`apps/api-worker`](../api-worker/overview.md) on 2026-07-31 (Monorepo 2.0 Tier 2).
> Routes live at `/universalis/*` (canonical) and `/api/v2/*` (compat) on data.xivdyetools.app,
> which also serves the legacy `proxy.xivdyetools.app` domain. See `DEPRECATIONS.md` for the
> cutover record and `apps/api-worker/src/universalis/` for the code.
