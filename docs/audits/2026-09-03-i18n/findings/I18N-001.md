# I18N-001: WITHDRAWN — og-worker's swatch card ignoring `slot`/`i` was already fixed before this audit's base was corrected
**Tier:** — · **Locale(s):** — · **Deploy unit:** og-worker · **Generated?** no

## Location
- `apps/og-worker/src/og-data-generator.ts:790-818` — the swatch case.

## Evidence
- Filed against worktree base `32e08207`, which was **two days stale**: local `main` predated `origin/main` (`cf79ac9f`) by the merges of PRs #158, #159, #160 and #161.
- On the real base the case reads `searchParams.get('slot') ?? searchParams.get('sheet')`, resolves the cell address via `resolveCellHex(sheet, cellIndex, race, gender)`, and keeps `hex`/`color` as the pre-5.0 alias — the exact fix this finding proposed. Commit `35914823` *"fix(og-worker): unfurl the cell a Swatch link actually shares"*, whose own comment names it BUG-021 and cites the orphaned localized sentences.
- Kept rather than deleted so the next audit does not rediscover it, and as the record of why an audit must pin its base commit.

## Fix
- None. Nothing to do.

## Status
WITHDRAWN 2026-09-03 — fixed upstream in `35914823` (merged to `origin/main` via PR #161).
