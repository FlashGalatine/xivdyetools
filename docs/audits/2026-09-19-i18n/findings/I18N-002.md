# I18N-002: `/manual topic` choices are English in every locale's command picker — `choiceLocalizations()` has no `manual`/`topic` case
**Tier:** P2 · **Locale(s):** ja de fr ko zh · **Deploy unit:** discord-worker · **Generated?** no

## Location
- `apps/discord-worker/src/commands/schemas.ts:554-561` — six choices (`📸 Image Matching Tips`, `♿ Colour Vision`, `🔲 Contrast`, `📐 Matching Methods`, `🪙 Spectrum & Prices`, `👤 Character File`).
- `apps/discord-worker/src/commands/localize.ts:124-181` — cases exist for preferences, harmony/type, accessibility/vision, budget/preset, dye/category; none for `manual`/`topic`, so it falls to `return undefined`.

## Evidence
- The localized topic names already exist: `manual5.topics.<topic>.name` in all six bot-logic locale files (rewritten 2026-09-18/19), and the `/manual` embed itself renders them. Only the picker is English.

## Fix
- Add a `manual`/`topic` case resolving `manual5.topics.<camelCase(value)>.name`, keeping the emoji prefix (Discord renders it; resvg never draws it). Extend `localize.test.ts` with the six choices × five locales. `register-commands` runs on merge.

## Status
OPEN
