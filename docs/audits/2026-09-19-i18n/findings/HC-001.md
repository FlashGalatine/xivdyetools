# HC-001: `/about` renders a hardcoded English sentence under a translated field name
**Tier:** P2 (user-visible) · **Locale(s):** ja de fr ko zh · **Deploy unit:** discord-worker (+ bot-logic key) · **Target set:** bot-logic · **Proposed key:** `about.builtOnBody`

## Location
- `apps/discord-worker/src/handlers/commands/about.ts:32` — `const BUILT_ON = 'Market prices from Universalis · Paint mixing by spectral.js';`
- `apps/discord-worker/src/handlers/commands/about.ts:135-136` — field `name: t.t('about.builtOn')` (localized), `value: BUILT_ON` (English).

## Evidence
- The constant's comment calls it "proper nouns", but only `Universalis` and `spectral.js` are; "Market prices from" / "Paint mixing by" are prose. A Japanese user sees a Japanese heading over an English sentence.

## Fix
- New key `about.builtOnBody` ×6 with the two brand names kept verbatim (e.g. `{universalis}` / `{spectral}` placeholders or literal). The Square Enix disclaimer (`ATTRIBUTION`, `about.ts:28`) stays English — verbatim legal text, not filed.

## Status
OPEN
