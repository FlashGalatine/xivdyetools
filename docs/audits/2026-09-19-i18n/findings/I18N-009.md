# I18N-009: published JSDoc says `extractLocaleCode('zh-CN')` returns `null` (it returns `'zh'`); api-worker spells the locale union literally
**Tier:** P3 · **Locale(s):** zh / all · **Deploy unit:** core (published), api-worker · **Generated?** no

## Location
- `packages/core/src/services/LocalizationService.ts:53` — `@example extractLocaleCode('zh-CN') // null (not supported)`; `:57-58` splits on `-` and returns `'zh'`. The doc ships in the npm package's `.d.ts`.
- `apps/api-worker/src/types.ts:48` — `locale: 'en' | 'ja' | 'de' | 'fr' | 'ko' | 'zh'` while `validation.ts:35-36` imports the type from core. Remainder of `2026-09-03-i18n/I18N-004` (locale lists spelled literally).

## Evidence
- Behaviour is the desired one (Discord's `zh-CN`/`zh-TW` and browsers' `zh-Hans` must map to `zh`); only the documentation is wrong. A seventh locale would compile in api-worker with the stale union.

## Fix
- Correct the example (`'zh-CN' // 'zh'`, and show `'pt-BR' // null`). `types.ts`: `locale: LocaleCode` via `import type`. Docs-only for core — ride along with the next core release, no bump of its own.

## Status
FIXED 2026-09-20 `f7c6e1fc` + `7ae472e7` — JSDoc example corrected; `Variables.locale` is `LocaleCode`
