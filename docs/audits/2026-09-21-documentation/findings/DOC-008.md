# DOC-008: contributing.md tells contributors to run `lint:fix`, which 15 of 17 workspaces do not define
**Severity:** MEDIUM · **Deploy unit:** repo-wide · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/developer-guides/contributing.md:65` — `pnpm --filter <project> run lint:fix`, given generically for any project.

## Evidence
- Only `apps/web-app` and `packages/core` define a `lint:fix` script (checked every `apps/*/package.json` and `packages/*/package.json`).
- For the other 15 workspaces the command fails with `Missing script: lint:fix` — a contributor following the guide hits an error, not a lint fix.

## Fix
- Either name the two workspaces that support it, or give the portable form (`pnpm --filter <project> exec eslint src --fix`) and note that `lint` itself is the gate.

## Status
OPEN
