# DOC-007: api-contracts.md omits the live `POST /auth/revoke` route
**Severity:** MEDIUM · **Deploy unit:** oauth · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/architecture/api-contracts.md:536-607` — the OAuth section documents `GET /auth/discord`, `GET|POST /auth/callback`, the struck-through `POST /auth/refresh` (removed in oauth 3.0.0) and `GET /auth/me`, and stops there.

## Evidence
- `apps/oauth/src/handlers/token.ts:92` — `POST /auth/revoke` handler; mounted at `apps/oauth/src/index.ts:286` (`app.route('/auth', tokenRouter)`), and listed in that file's own route map comment at `:276`.
- So the contract document names a route that no longer exists and omits one that does. A consumer reading it has no documented way to revoke a session.

## Fix
- Add a `### POST /auth/revoke` section beside `GET /auth/me`, with its request shape, auth requirement and status codes taken from `token.ts:92`.

## Status
OPEN
