# DOC-013: components.md gives the wrong tool-rail order — Mixer is 6th, not last
**Severity:** MEDIUM · **Deploy unit:** web-app · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/projects/web-app/components.md:46` — the rail order is given as "Harmony, Extractor, Vision, Compare, Gradient, Presets, Budget, Swatch, Mixer".

## Evidence
- `apps/web-app/src/components/v4/v4-app-header.ts` — the `TOOL_MENU` array, which drives both the desktop rail and the mobile title-menu in array order, is: `harmony, extractor, accessibility, comparison, gradient, mixer, presets, budget, swatch`.
- So `mixer` sits 6th, directly after `gradient`; the document places it 9th and shifts `presets`, `budget` and `swatch` one position each.

## Fix
- Reorder the list to match `TOOL_MENU`. The same line's claim that later keyboard order diverges from rail order was checked and is correct — leave that.

## Status
OPEN
