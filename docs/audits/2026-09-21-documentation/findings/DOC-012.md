# DOC-012: two web-app docs state 1,152 locale keys per language; the real count is 1,129
**Severity:** LOW · **Deploy unit:** web-app · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/projects/web-app/components.md:197` — "(1,152 keys per language, parity-validated across `en ja de fr ko zh`)".
- `docs/projects/web-app/overview.md:27` — "the UI locale files grew to 1,152 keys × 6".

## Evidence
- Counting leaf strings in `apps/web-app/src/locales/en.json` gives **1,129**; the project's own `scripts/i18n-parity.mjs` reports the same. Stale by 23 in both places.
- This branch changed 41 **values** and no keys, so the count is the same on `main`.

## Fix
- Both numbers → 1,129. Better: drop the figure — it goes stale on every key change, and the parity gate already asserts the six files agree.

## Status
OPEN
