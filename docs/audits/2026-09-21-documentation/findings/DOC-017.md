# DOC-017: moderation-worker's file tree omits `types/modal.ts`
**Severity:** LOW · **Deploy unit:** moderation-worker · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/projects/moderation-worker/overview.md:102-105` — the Project Structure tree lists `types/env.ts`, `ban.ts` and `preset.ts`.

## Evidence
- `apps/moderation-worker/src/types/` contains `ban.ts env.ts modal.ts preset.ts`. `modal.ts` backs the modal handlers and is absent from the tree.

## Fix
- Add `│   ├── modal.ts               # Modal (submission) types` to the tree, in file order.

## Status
OPEN
