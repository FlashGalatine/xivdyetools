# DOC-010: the bot FAQ claims dye autocomplete needs 2 characters; it fires on 1
**Severity:** MEDIUM · **Deploy unit:** discord-worker · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/user-guides/discord-bot/faq.md:37` — "Typing at least 2 characters", listed as a requirement for autocomplete to return suggestions.

## Evidence
- `apps/discord-worker/src/index.ts:1166` — `const matchingDyes = query.length >= 1 ? searchDyesByName(query, locale) : dyeService.getAllDyes();`. One character is enough, and an empty query returns the full list rather than nothing. No 2-character gate exists anywhere in the autocomplete path.
- Graded MEDIUM, not HIGH: the advice is materially wrong but not harmful — a reader types one more character than needed.

## Fix
- Replace with "Typing at least 1 character (an empty box lists every dye)".

## Status
OPEN
