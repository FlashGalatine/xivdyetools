# DOC-014: components.md quotes a sidebar control as "Colour wheel"; the shipped label is "Color wheel"
**Severity:** MEDIUM · **Deploy unit:** web-app · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/projects/web-app/components.md:47` — "the Harmony **Colour wheel** select", written as the control's name.

## Evidence
- `apps/web-app/src/locales/en.json:174` — `"colorWheel": "Color wheel"`. `git show origin/main:…` gives the same value, so the label was already American before this branch; the document has never matched it.
- This is a stale **quotation**, not a spelling slip: a reader scanning the sidebar for "Colour wheel" finds "Color wheel". Filed separately from DOC-001 for that reason.
- Not the same as `docs/projects/web-app/tools.md:31` ("**Colour wheels.**"), which is a generic heading rather than a quoted label — that one is ordinary British spelling and belongs to DOC-001.

## Fix
- Quote the label as `Color wheel`.

## Status
OPEN
