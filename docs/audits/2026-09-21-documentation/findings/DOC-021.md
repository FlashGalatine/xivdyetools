# DOC-021: the multi-color-extraction spec shows `PaletteService` signatures that no longer compile
**Severity:** MEDIUM · **Deploy unit:** core · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/specifications/multi-color-extraction.md:120` — `extractPalette(pixels: RGB[], colorCount: number): RGB[]`
- `:129-133` — `extractAndMatchPalette(…)` with `dyeService` last
- `:345` — the worked example `paletteService.extractPalette(pixels, 3)`

## Evidence
- `packages/core/src/services/PaletteService.ts:368` — `extractPalette(pixels: RGB[], options: PaletteExtractionOptions = {}): ExtractedColor[]`. Both the second parameter and the return type differ; `extractPalette(pixels, 3)` passes `3` where an options object is expected.
- `:448` — `extractAndMatchPalette(pixels, dyeService, options = {})` takes `dyeService` **second**, not last.
- Its sibling specs self-annotate divergence ("As shipped:", "Not shipped."); this section presents the signatures as current and backs them with a runnable example, so a reader will copy them.

## Fix
- Update both signatures and the `:345` example to the real shapes, or mark the section "As specified — see `PaletteService.ts` for the shipped signatures".

## Status
OPEN
