# DOC-019: OPEN_ITEMS.md still lists cross-identity bans as unshipped work
**Severity:** MEDIUM · **Deploy unit:** repo-wide · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/operations/OPEN_ITEMS.md:122-123` — "cross-identity (`xivauth_id`) bans need oauth + moderation-worker changes (FINDING-017)".

## Evidence
- `apps/presets-api/src/middleware/ban-check.ts:26-34` records the 2026-09-16 ruling: XIVAuth-only accounts are bound by the oauth UUID, and **moderation-worker (Sprint 6) already bans and unbans that target** — `apps/moderation-worker/src/handlers/.../preset.ts:564` switches the label accordingly.
- What actually remains is narrower and is documented at `ban-check.ts:36-49`: a banned XIVAuth user can shed the ban by later linking Discord.
- As written the item is overbroad: someone picking it up would redo shipped work before finding the real residual.

## Fix
- Replace the item with the residual only — "a banned XIVAuth-only user sheds the ban by later linking Discord (`ban-check.ts:36-49`)" — and mark the rest shipped 2026-09-16.

## Status
OPEN
