# DOC-015: the docs call the presets tool "Community Presets"; the app has always titled it "Preset Palettes"
**Severity:** MEDIUM · **Deploy unit:** web-app · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/user-guides/web-app/community-presets.md:1,5` — the guide's title and opening sentence.
- `apps/web-app/CLAUDE.md:21` — a table whose column is **Title** gives "Community Presets".
- 23 living documents in total use the name; the full list is in `evidence/review-web-app.md`.

## Evidence
- `apps/web-app/src/locales/en.json` — `tools.presets.title` = **"Preset Palettes"**, `shortName` = "Presets". `router-service.ts` wires that key to the document title and the mobile menu.
- `git log -S'"title": "Preset Palettes"'` shows no earlier value: the title has **always** been "Preset Palettes". This is a long-standing divergence, not a rename the docs missed.
- The string "Community Presets" appears in `apps/web-app/src` only in two JSDoc comments (`shared/tool-config-types.ts:188`, `components/v4/config-sidebar.ts:1570`) — never as rendered text.

## Fix
- **Maintainer's call which side moves.** Either rename the tool's `title` to the name every document uses, or change the documents to the shipped title. Do not do both halves in different directions. Whichever is chosen, `apps/web-app/CLAUDE.md:21`'s Title column must end up matching `tools.presets.title`.

## Status
OPEN
