# DOC-004: architecture and developer guides are British English — 28 spellings across 7 documents
**Severity:** LOW · **Deploy unit:** repo-wide · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/architecture/overview.md` — 16
- `docs/developer-guides/contributing.md` — 4
- `docs/architecture/api-contracts.md` — 3
- …and 4 more document(s) — the full per-document table is `evidence/spelling-confirmed.md`

## Evidence
- `node .agents/skills/audit-shared/scripts/american-spelling.mjs .` → exit 1; full table in `evidence/american-spelling.txt`, per-document counts in `evidence/spelling-confirmed.md`.
- 27 confirmed candidates (28 raw, **1 rejected**). The standard, the carve-outs and the glossary exception are `.agents/skills/audit-shared/american-english.md`.
- **Rejected:** `architecture/api-contracts.md:436`'s "colours" is inside a `/categories` example response and quotes a live seed value verbatim — `apps/presets-api/schema.sql:24` stores `'Palettes built around a character''s own colours'`. Correcting the document would make it misquote the database.
- `architecture/overview.md` (16) includes prose inside mermaid and ASCII-tree fences — the `diagram` zone, which readers see rendered.

## Fix
- `node .agents/skills/audit-shared/scripts/american-spelling.mjs . <files> --fix` applies only prose zones and holds every `glossary?` / `multilingual-row?` hit for review.
- Re-read any line that quotes a UI label: the label must match `en.json` as shipped, not the standard.

## Status
OPEN
