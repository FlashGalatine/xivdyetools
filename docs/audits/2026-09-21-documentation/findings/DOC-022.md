# DOC-022: the two reference documents disagree on "Market Board"
**Severity:** LOW · **Deploy unit:** repo-wide · **Baseline:** working tree = `main` `59572c0` + this branch's three spelling commits

## Location
- `docs/reference/glossary.md:91` — the heading `### Marketboard` (one word).

## Evidence
- `docs/reference/ffxiv-terminology.md:229` fixes the official EN client term as **"Market Board"** (two words), and `:240` records that the apps mean the feature ("Market Board prices").
- The terminology file is the outcome of the dedicated 2026-09-19/20 terminology work, so it is the authority; the glossary heading predates it.

## Fix
- Rename the heading to `### Market Board` and keep a redirect line if anything links the old anchor — `docs:check-links` will catch a broken anchor only if it is a relative link.

## Status
OPEN
