# Documentation audit — cluster: operations, maintainer, reference, specifications, entry pages

| cand-id | sev | file:line | one-line claim | evidence pointer |
|---|---|---|---|---|
| MOD-01 | MEDIUM | docs/operations/MODERATION.md:37,100 | `/preset ban_user`/`unban_user` usage shown as `user:<discord_user>` (implies a Discord-mention picker); doc never mentions banning an XIVAuth-only (no-Discord) author | apps/moderation-worker/scripts/register-commands.ts:93-98 (`user` option is `OptionType.STRING` + `autocomplete: true`, "search by username" — not a USER-mention type); preset.ts:564 (`ban.discordId` vs `ban.xivauthId` label — the command already targets non-Discord identities) |
| OPEN-01 | MEDIUM | docs/operations/OPEN_ITEMS.md:122-123 | Lists "cross-identity (`xivauth_id`) bans need oauth + moderation-worker changes" as still-open work | apps/presets-api/src/middleware/ban-check.ts:26-34 — 2026-09-16 fix wave: moderation-worker already bans/unbans an XIVAuth-only target by UUID (also `preset.ts:564`); only the narrower "ban survives later linking Discord" gap (ban-check.ts:36-49, labelled B1/residual) remains, not the blanket "still needs changes" OPEN_ITEMS states |
| GLOS-01 | MEDIUM | docs/reference/glossary.md:94-95 | "Special Dye: Rare dyes obtained from crafting, dungeons, or events. Often more expensive." | docs/maintainer/adding-dyes.md:279 ("`Special` \| Metallic, pastel, dark series"); packages/core/src/data/dyes.json — the 27 `category:"Special"` rows are Gunmetal Black / Pastel* / Dark* etc., acquired only via Venture Coffers / Cosmic Exploration / The Firmament — "crafting" and "dungeons" are not valid `DYE_ACQUISITIONS` values at all (dye-vocabulary.ts:34-39) |
| MCE-01 | MEDIUM | docs/specifications/multi-color-extraction.md:120,129-133 | Shows shipped `PaletteService.extractPalette(pixels, colorCount: number)` / `extractAndMatchPalette(pixels, colorCount: number, dyeService)` as the core API | packages/core/src/services/PaletteService.ts:368 (`extractPalette(pixels, options: PaletteExtractionOptions = {})`) and :448-451 (`extractAndMatchPalette(pixels, dyeService, options = {})`) — `dyeService` is positional param 2, and `colorCount` lives inside an `options` object, not a bare param |
| GLOS-02 | LOW | docs/reference/glossary.md:91 | "Marketboard" (one word) | docs/reference/ffxiv-terminology.md:229 — the dedicated 2026-09-19/20 terminology research fixed the official EN client term as "Market Board" (two words) |

POSITIVE:
- 17-project count (8 packages + 9 apps), 125-dye/7-field/stainID schema, and 11-entry `facewearColors` (id/name/hex) all verified byte-for-byte against `packages/*`, `apps/*`, `dyes.json`, `facewear_colors.json`.
- `ffxiv-terminology.md`'s locale-derived tables (races 8, clans 16, harmonyTypes 10, visionTypes 5, colorWheels 5, facewearColors 11, categories 9, acquisitions 7, tools 6, sheets 9, meta 3, labels 7) and every spot-checked EN/JA value match `core/src/data/locales/{en,ja}.json` exactly, key for key.
- `DEPLOY_ENVIRONMENTS.md`'s per-worker bare-`deploy` table (incl. oauth's inversion and og-worker's routed-beta exception) verified against all 7 `apps/*/wrangler.toml` files — fully accurate, including exact file:line citations in the sibling `DOMAIN_DEPRECATION.md`.
- `feature-roadmap.md` / `specifications/index.md` / `collections.md` numbers (15 curated presets across 3 categories, 22 `/budget quick` picks, 40 favorites / 50 collections / 20 dyes-per-collection) all match source exactly.
- specifications/*.md consistently self-annotate shipped-vs-proposed divergence ("As shipped:", "Not shipped.") instead of silently going stale — verified across all six.
- discord-worker's 17-entry `COMMAND_REGISTRY` matches every "17 registered slash commands" claim across the cluster.

SPELLING-EXCEPTION: Confirmed correct, not to be "fixed" — `dyes.json`'s 4 dye names ("Ash Grey", "Goobbue Grey", "Slate Grey", "Charcoal Grey") and `facewear_colors.json`'s `grey` slug all correctly use "Grey"; `ffxiv-terminology.md`'s Facewear Colors table (line 116) and the "Glamour" terms throughout reference/ are likewise the correct official spellings per the task's own pre-clearance. No British→American pass should touch any of these.

COVERED: 26/26 read.
- Full: CLAUDE, README, index, versions (both halves); maintainer/{adding-dyes,dye-maintainer-tool,index}; operations/{ANALYTICS_QUERIES,DEPLOY_ENVIRONMENTS,DOMAIN_DEPRECATION,IMAGE_WORKER_SPLIT,MODERATION,OPEN_ITEMS,SECRET_ROTATION,index,security-remediation-2026-09-15}; reference/{ffxiv-terminology,glossary,index}; specifications/{budget-aware-suggestions,feature-roadmap,index}.
- Partial (header/status block + targeted verification of numeric/API claims, not exhaustive line-by-line): specifications/{collections,community-presets,multi-color-extraction,preset-palettes} — no further issues found beyond MCE-01.
