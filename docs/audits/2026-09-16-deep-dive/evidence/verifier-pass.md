# Opus verification pass — rows graded MEDIUM or above (verbatim returns)

Prompt contract: open each `file:line`, confirm or reject on what the file says, assign the severity you would give, ≤ 2-line reason, read-only. Two batches.

## Batch 1 (13 rows)

| id | verdict | severity | reason |
|---|---|---|---|
| webapp-tools-a-01 | REJECTED | LOW | Lines + "no `navigate-to-tool` listener anywhere" are true, but nothing ever emits the legacy actions: result-card.ts:1792-1841 only fires `inspect-*`/`transform-*`/`external-*` and handles them itself (1127-1167 → `handoffTo`/`addToTool`/`navigateTo`), so swatch's 2375-2416 and mixer's 2016-2077 branches (and their toasts) are unreachable dead code, not a live no-op. |
| webapp-tools-a-02 | CONFIRMED | MEDIUM | swatch-tool.ts:1546 is a bare `void import(...).then()`; the two sibling call sites (config-sidebar.ts:1743-1753, chara-import.ts:498-504) both `.catch`, and changelog-modal.ts:111 records that the app registers no global `unhandledrejection` handler (BUG-090) — so a stale-deploy chunk 404 is a silent dead "submit palette". |
| webapp-tools-b-01 | CONFIRMED | MEDIUM | extractor-tool.ts contains zero `ShareService`/`share-button`/`URLSearchParams` references — it neither generates nor *consumes* share links, while og-worker/src/index.ts:1047 still serves `/og/extractor/:colors.png` and share-service.ts:114/136 keeps `ExtractorShareParams` (referenced only by its own union). No v4 shell path supplies it; every other tool wires its own `v4-share-button`. |
| presets-api-01 | CONFIRMED | MEDIUM | Net claim holds: ban-check.ts:30-35 matches `discord_id` only, auth.ts:71-77 falls back to the `sub` UUID, and both ban entry points snowflake-gate the target (preset.ts:521/621, ban-confirmation.ts:16/86); XIVAuth login is live (auth-service.ts:705). Correction: `xivauth_id` *is* read (ban-service.ts:174/187/194/649, types/ban.ts:14) — it is simply never written by any ban path. |
| pkg-svg-bot-logic-01 | CONFIRMED | MEDIUM | swatch.ts:248-252 gates `·L`/`·R`/`·LR` behind `!offGrid`, and SLOT_KEYS:119-120 maps both eyes to `card.slotEyes`, so heterochromia + off-grid yields two rows labelled EYES / OFF GRID with different dyes and no left/right cue. |
| pkg-worker-kit-test-utils-16 | CONFIRMED | MEDIUM | dye.ts:33-35 draws `Math.random()*254+1` with no uniqueness and line 181-183 derives `itemID`/`id` from it, so two `createMockDye()` calls collide ~1/254 (~0.4 %/run) and dye.test.ts:90-95 `expect(dye1.id).not.toBe(dye2.id)` is genuinely flaky. (Read-only run — the "failed in baseline" part is unverified by me.) |
| webapp-services-03 | CONFIRMED | LOW | gradient-tool.ts:23 imports `RouterService` from `@services/router-service` while gradient-tool.test.ts:201-205 stubs it only on `@services/index` (index.ts:26 re-exports it), so the stub is inert. Correction: v4/preset-tool.ts and welcome-modal.ts have no test files at all, and no test asserts `navigateTo`, so nothing passes vacuously today — a latent trap, not a live false green. |
| core-color-01 | CONFIRMED | LOW | hexToHsv (445-455) keys the LRU off `normalizeHexKey` (197, strips `#`) and returns a hit before `hexToRgb` (208-214) validates; `PATTERNS.HEX_COLOR` (constants/index.ts:116) requires the `#`, so bare `'FF0000'` throws cold and succeeds warm. The warm value is still the correct colour, so it is a consistency defect only. |
| discord-handlers-05 | CONFIRMED | MEDIUM | manual.ts:338 `await resolveLodestoneRegion` runs before a `type: 4` reply (line 346); on a cold isolate 255-265 awaits `getCachedWorlds`/`getCachedDataCenters`, whose `request()` uses `REQUEST_TIMEOUT = 10000` (universalis-client.ts:79) with no shorter budget — a slow proxy blows Discord's 3 s ack. Narrow trigger (topic `spectrum_prices` + a saved world), so operational risk rather than a common wrong result. |
| discord-core-01 | CONFIRMED | LOW | index.ts:276-281 parses `content-length` (absent/chunked → 0 → passes) then 284-287 `await c.req.json()` with no streaming cap. Reachable only after the constant-time `INTERNAL_WEBHOOK_SECRET` bearer check at 270-274, and the only caller is presets-api over a service binding. |
| oauth-12 | CONFIRMED | LOW | The env-validation middleware (119-140) returns `c.json({error:'Service misconfigured'},500)` without calling `next()`, so the headers middleware registered at 144-165 never runs on it — no nosniff/no-store/HSTS. Body carries no secrets and the worker is already wholly broken in that state. |
| presets-api-03 | CONFIRMED | LOW | presets.ts:349 `.bind(auth.userName, auth.userDiscordId)` is unguarded where 934 uses `\|\| 'Unknown User'` and 1181 `?? ''`. Reaching it needs an undefined `userName`: the web path sets `global_name \|\| username` (auth.ts:367) and `username` is a required JWT claim (types/auth/jwt.ts:60), and the bot never calls `/refresh-author` — so a malformed/legacy token only. |
| discord-handlers-03 | REJECTED | LOW | The push at preset.ts:820-826 is correct for the grammar: submit compacts options into a dense array (425-431) and the API floor is 3 dyes, so there are no positional slot semantics and a 2-dye preset cannot exist. The only defect is the stale comment at line 712 ("positions map onto the stored 3-6 stainIDs"). |

## Batch 2 (5 rows)

| id | verdict | severity | reason |
|---|---|---|---|
| webapp-v4-19 | CONFIRMED (with correction) | MEDIUM | `priceData` (127–133, 626–641, 680–710) is written but never read by `render()`/helpers — dead state + a live Universalis fetch whenever Show prices is on. But the claim that no `.dye-price` markup is emitted is wrong: line 958 does emit `.dye-price`, showing static vendor `formatGil(dye.cost)` / "not sold". So nothing wrong is displayed — the opt-in setting is simply a no-op here, which is MEDIUM, not HIGH. |
| webapp-v4-20 | REJECTED | LOW | No generation guard is real (658–675), but the A→B path is unreachable: `preset-tool.ts:1080-1095` renders `<v4-preset-detail>` in a separate template branch, so leaving a detail sets `selectedPreset = null` (814) and lit destroys/recreates the element — A's late response writes to a detached instance. Reachable variant only: an in-flight check from `connectedCallback`/`updated` clobbering an optimistic `handleVote` result. |
| webapp-v4-21 | CONFIRMED | LOW | `disconnectedCallback` (430–443) clears five unsubscribes but not `_searchDebounce` (set 886–893). The late `loadPresets()` (482–512) only sets local reactive state on a detached element plus one wasted API call; its persistent writer `reconcileTombstones()` (526+) returns early while `searchQuery` is non-empty, which is exactly the fired-debounce case. |
| webapp-v4-22 | CONFIRMED | MEDIUM | `handlePresetSelect` (762–766) pushes `{ preset: id }` with no `toolId`; `RouterService.handlePopState` (356–375) falls back to `parseCurrentPath()` → still `presets` and notifies unconditionally, and `v4-layout.ts:316-321` → `loadToolContent` (513+) clears the container and does `document.createElement('v4-preset-tool')` for the `presets` case (658–668) with no same-tool skip. Back therefore remounts the tool: `tab` (92), `searchQuery` (132), scroll and the loaded pool all reset + refetch. |
| webapp-modals-05 | CONFIRMED (latent) | LOW | Line 118 drops ids `resolvePresetDye` can't resolve and 698 then diffs `dyes.join(',')` against `preset.dyes.join(',')`, so a shortened `dyes` patch would go out on a no-edit Save. Unreachable today: `dyes.json` holds exactly 125 contiguous stainIDs 1–125 and `resolvePresetDye` (dye-service-wrapper.ts:57-63) resolves every one; only an out-of-DB 126–254 id (which presets-api accepts) or a DB shrink triggers it — and a total wipe would be caught by the `MIN_DYES` check (721). |

## Coordinator's own re-check of the two flips

- **webapp-tools-a-01** — `result-card.ts:100-118` (`ContextAction` union) and its two `emit('context-action', …)` sites (1099, 1177) carry only `inspect-*`, `transform-*`, `external-*`, `add-mixer-slot-*`; swatch-tool's `case 'add-comparison'` family at 2373-2416 can never match. Filed as REFACTOR-001 (delete), not a BUG.
- **webapp-v4-19** — accepted MEDIUM: the row shows the vendor price, the toggle is a no-op. Filed as BUG-004.
