# BUG-011: `CharacterMatchOptions.matchingMethod` documents default `'oklab'` while the code defaults to `'ciede2000'`
**Severity:** LOW · **Type:** Logic · **Deploy unit:** core · **Covered by test?** n/a

## Location
- `packages/core/src/services/CharacterColorService.ts:49` — `/** Color matching algorithm (default: 'oklab') */`
- `packages/core/src/services/CharacterColorService.ts:327` — `const { count = 3, matchingMethod = 'ciede2000' } = options`

## Evidence
- Reviewer row `core-data-04`; the published `.d.ts` carries the wrong default to API consumers

## Fix
- Fix the comment (ciede2000 is the 5.0 default everywhere else); no behaviour change

## Status
FIXED (Sprint 8, `5e88e2f8`) — JSDoc default corrected to `'ciede2000'`; ships in the 5.3.0 `.d.ts`.
