# BUG-027: `v4-preset-tool` never clears its search debounce in `disconnectedCallback`, so one `loadPresets()` fetch fires into a detached element
**Severity:** LOW · **Type:** Resource · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/components/v4/preset-tool.ts:431-443` vs `886-893` — five unsubscribes cleared, `_searchDebounce` not

## Evidence
- Reviewer row `webapp-v4-21`; Opus pass: `reconcileTombstones()` returns early while `searchQuery` is non-empty, so no persistent write

## Fix
- `clearTimeout(this._searchDebounce)` in `disconnectedCallback`

## Status
FIXED 2026-09-16 8050c90f — `clearTimeout(_searchDebounce)` in disconnectedCallback
