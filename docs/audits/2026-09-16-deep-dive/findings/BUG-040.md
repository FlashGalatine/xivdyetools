# BUG-040: Three components value-import `RouterService` past the `@services/index` barrel their tests mock, so the mock is inert (the PR #161 trap, three survivors)
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/components/gradient-tool.ts:23` — `from '@services/router-service'`; `__tests__/gradient-tool.test.ts:44,201-205` mocks `@services/index` only
- `apps/web-app/src/components/v4/preset-tool.ts:34`, `welcome-modal.ts:21` — same import; these two have no test file at all

## Evidence
- Reviewer row `webapp-services-03`; Opus pass: no test asserts `navigateTo` today, so nothing passes vacuously yet — a latent trap

## Fix
- Import from the barrel (the documented rule); add an eslint `no-restricted-imports` entry for `@services/router-service` outside services/

## Status
FIXED (partial) 2026-09-16 8050c90f — gradient-tool + preset-tool (+ v4-layout) import from the barrel, ESLint `no-restricted-imports` active; welcome-modal.ts EXEMPTED: the barrel import grows the modals chunk 274.7→367.8 KB past its 280 KB budget — residual latent trap, revisit with the bundle budget
