# BUG-016: moderation-worker's presets-api client fetches carry no `AbortSignal`, unlike every Discord-facing call
**Severity:** LOW · **Type:** Resource · **Deploy unit:** moderation-worker · **Covered by test?** no

## Location
- `apps/moderation-worker/src/services/preset-api.ts:112-127` — `env.PRESETS_API.fetch(new Request(…))` and the HTTP fallback, no timeout; `utils/discord-api.ts` uses `AbortSignal.timeout` everywhere (BUG-040)

## Evidence
- Reviewer row `moderation-worker-01` (filed as OPT there); a hang leaves a `waitUntil`-wrapped moderation action without a terminal state

## Fix
- `signal: AbortSignal.timeout(10_000)` on both branches; classify the abort as `upstream_presets`

## Status
FIXED (Sprint 6, `c6c8c5f7` + `64391c47`) — `AbortSignal.timeout(10_000)` on both branches, the service-binding one as `env.PRESETS_API.fetch(url, init)` (the shape this worker already runs in production); the abort surfaces through the existing `PresetAPIError` path. No `upstream_presets` class exists in this worker, so none was added.
