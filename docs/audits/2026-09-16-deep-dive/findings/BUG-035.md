# BUG-035: moderation-worker rate-limit KV-error test asserts only `resolves.not.toThrow()`
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** moderation-worker · **Covered by test?** the test cannot fail

## Location
- `apps/moderation-worker/src/middleware/rate-limit.test.ts:236` — 'handles KV errors gracefully'

## Evidence
- Reviewer row `moderation-worker-02`. Behaviour it was meant to catch: fail-open returning `allowed:true` **and** `backendError:true` (FINDING-012)

## Fix
- Assert the returned `{ allowed, backendError }` shape and that the warning is logged

## Status
OPEN
