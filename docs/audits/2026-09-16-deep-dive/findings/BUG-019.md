# BUG-019: api-worker's SWR expiry `waitUntil(cache.delete())` has no `.catch()`, unlike its `storeAsync` sibling
**Severity:** LOW · **Type:** Error handling · **Deploy unit:** api-worker · **Covered by test?** no

## Location
- `apps/api-worker/src/universalis/services/cache-service.ts:118` — `this.ctx.waitUntil(cache.delete(cacheRequest))`

## Evidence
- Reviewer row `api-worker-03`; only unhandled-rejection noise, no user effect

## Fix
- `.catch(() => {})` with a debug log, matching `storeAsync`

## Status
FIXED (Sprint 11, `3e6d9a46`) — `.catch` with an optional structural debug logger (no caller threads one yet; `storeAsync` never logged either).
