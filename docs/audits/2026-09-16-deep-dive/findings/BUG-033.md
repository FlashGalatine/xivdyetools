# BUG-033: `/gradient`'s adapter (`commands/gradient.ts`) has no test file
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** discord-worker · **Covered by test?** no

## Location
- `apps/discord-worker/src/handlers/commands/gradient.ts` — no `gradient.test.ts`; option parsing, distinct-dye rule and the 3-stage cap are unexercised

## Evidence
- Reviewer row `discord-handlers-01`; discord-worker sits at 88 % statements largely because of these two files

## Fix
- Add a suite mirroring `mixer`/`comparison` shape: happy path, duplicate dyes rejected, > 3 stages capped, render failure marks `render`

## Status
FIXED (Sprint 4, `18e86d3c` + `feae1b39` + `fd5689a7`) — `gradient.test.ts` added and the file removed from the coverage `exclude`. The duplicate-dye rule and 3-stage cap this finding describes do not exist in the adapter (the cap is bot-logic's row cap); the suite documents actual behaviour, including `steps: 0` → 6 via `|| 6` (unreachable from Discord: schema `min_value` 2).
