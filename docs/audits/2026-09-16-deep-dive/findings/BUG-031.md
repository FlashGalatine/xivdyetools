# BUG-031: stoat-worker `/ping` builds its latency string before `sendMessage` resolves, so it always reports ~0 ms
**Severity:** LOW · **Type:** Logic · **Deploy unit:** stoat-worker (parked — P3) · **Covered by test?** no

## Location
- `apps/stoat-worker/src/commands/ping.ts:9-11` — `Date.now() - startTime` evaluated while building the payload

## Evidence
- Reviewer row `image-stoat-14`

## Fix
- Send, then edit the message with the measured round-trip — or drop the number

## Status
FIXED (Sprint 12, `19e36205`) — measured after the send and edited into the message (revolt.js `Message.edit` verified).
