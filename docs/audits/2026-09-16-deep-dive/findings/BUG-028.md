# BUG-028: `BaseComponent.handleRetry()` / `renderError()` never `unbindAllEvents()`, so a `safeAsync` fail→retry cycle leaks listener-map entries
**Severity:** LOW · **Type:** Resource · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/components/base-component.ts:306-323,419-432` — unlike `update()`/`handleReset()`

## Evidence
- Reviewer row `webapp-modals-01`; `git grep 'safeAsync('` finds no caller outside the base class — latent

## Fix
- Call `unbindAllEvents()` before `renderError()` and in `handleRetry()`; or delete `safeAsync` if it stays unused (dead-code candidate)

## Status
OPEN
