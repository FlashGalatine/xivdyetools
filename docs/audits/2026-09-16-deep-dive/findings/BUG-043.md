# BUG-043: presets-api ban-check tests pass through a D1 mock that special-cases the query and ignores which id and column were bound
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** presets-api · **Covered by test?** the tests cannot see BUG-001

## Location
- `apps/presets-api/tests/middleware/ban-check.test.ts`; `packages/test-utils/src/cloudflare/d1.ts:242-252`

## Evidence
- Reviewer row `presets-api-02`. Behaviour it was meant to catch: a banned identity that the query cannot match (BUG-001)

## Fix
- Drive the check with a scripted row keyed on the bound value; add the UUID-`sub` case alongside the snowflake one

## Status
OPEN
