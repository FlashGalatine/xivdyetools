# BUG-039: 'merges displayOptions rather than replacing them' asserts `not.toThrow()` only, in two tool suites
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** web-app · **Covered by test?** the tests cannot fail

## Location
- `apps/web-app/src/components/__tests__/extractor-tool.test.ts:448`
- `apps/web-app/src/components/__tests__/accessibility-tool.test.ts:816`

## Evidence
- Reviewer rows `webapp-tools-b-02/03`. Behaviour they were meant to catch: a `setConfig` that replaces instead of merging, dropping the other tool's display flags

## Fix
- Read the merged config back and assert both the changed and the untouched key

## Status
OPEN
