# BUG-013: `/webhooks/preset-submission` trusts `Content-Length` then buffers `c.req.json()` with no streaming cap
**Severity:** LOW · **Type:** Resource · **Deploy unit:** discord-worker · **Covered by test?** no

## Location
- `apps/discord-worker/src/index.ts:276-281` — `parseInt(content-length || '0')`; absent or chunked → 0 → passes
- `apps/discord-worker/src/index.ts:284-287` — `await c.req.json()` unbounded; the sibling `/webhooks/github` counts streamed bytes (FINDING-003, PR #183)

## Evidence
- Reachable only after the constant-time `INTERNAL_WEBHOOK_SECRET` check at 270-274, and the only caller is presets-api over a service binding — hence LOW
- Reviewer row `discord-core-01`; Opus pass confirmed LOW

## Fix
- Reuse the github route's bounded reader (`readTextCapped`, already in `swatch.ts`) with the 10 KB cap

## Status
OPEN
