# BUG-036: api-worker `serializeDye` test asserts its boolean/enum fields via `.toBeDefined()` only
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** api-worker · **Covered by test?** weakly

## Location
- `apps/api-worker/tests/lib/dye-serializer.test.ts:18-23`

## Evidence
- Reviewer row `api-worker-01`. Behaviour it was meant to catch: a field swap (`isPastel`/`isDark`) or a consolidation-type mislabel in the public JSON

## Fix
- Snapshot one metallic, one pastel, one Type-C dye against literal expected objects

## Status
OPEN
