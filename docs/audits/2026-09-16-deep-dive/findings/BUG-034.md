# BUG-034: `/mixer`'s adapter (`commands/mixer-v4.ts`) has no test file
**Severity:** LOW · **Type:** Untested behaviour · **Deploy unit:** discord-worker · **Covered by test?** no

## Location
- `apps/discord-worker/src/handlers/commands/mixer-v4.ts` — no test; the RYB default mode that BUG-006 (2026-09-02) broke is exercised only through bot-logic's own tests

## Evidence
- Reviewer row `discord-handlers-02`

## Fix
- Add a suite: mode option → bot-logic call args, ratio bounds, render failure → `render` outcome

## Status
FIXED (Sprint 4, `3180cba4` + `feae1b39` + `fd5689a7`) — `mixer-v4.test.ts` added (RYB default asserted explicitly; all six modes; render failure → `render`) and the file removed from the coverage `exclude`. `/mixer` has no ratio option; the suite covers `matching` instead. Note: the suite mocks `@xivdyetools/bot-logic` wholesale, so it is not a regression net for the bot-logic bump.
