# BUG-012: `/stats health` hard-codes `workerEnv = 'production'`, so the beta bot reports itself as production — and the test pins the literal
**Severity:** LOW · **Type:** Logic · **Deploy unit:** discord-worker · **Covered by test?** yes, but the assertion enshrines the bug

## Location
- `apps/discord-worker/src/handlers/commands/stats.ts:556` — `const workerEnv = 'production';` while `env.ENVIRONMENT` is `development` on `xivdyetools-discord-worker-dev`
- `apps/discord-worker/src/handlers/commands/stats.test.ts:892` — `expect(configField!.value).toContain('production')`

## Evidence
- Reviewer row `discord-handlers-06`

## Fix
- Read `env.ENVIRONMENT ?? 'unknown'`; change the test to drive both values

## Status
OPEN
