# BUG-025: `AuthService.isAuthenticated()` fire-and-forgets `logout()` with no re-entrancy guard, so N synchronous callers on an expired token trigger N revoke requests and notify storms
**Severity:** LOW · **Type:** Race · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/services/auth-service.ts:589-603` — `void this.logout()` inside a synchronous getter with real call sites in preset-detail, community-preset-service, preset-submission-service and config-sidebar

## Evidence
- Reviewer row `webapp-services-02`; `/auth/revoke` is idempotent so the duplicates are harmless, only wasteful

## Fix
- Keep a `logoutPromise` and return it while in flight; clear it in `finally`

## Status
OPEN
