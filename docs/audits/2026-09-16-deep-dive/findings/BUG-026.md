# BUG-026: An in-flight `checkVoteStatus()` started by `updated()` can clobber an optimistic `handleVote` result on the preset detail view
**Severity:** LOW · **Type:** Race · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/components/v4/preset-detail.ts:644-675` — no request-generation guard on `hasVoted` / `currentVoteCount` writes

## Evidence
- The reviewer's A→B navigation variant (`webapp-v4-20`) was REJECTED by the Opus pass: the detail element is destroyed on navigation (`preset-tool.ts:814,1080-1095`); only the same-instance vote-then-check ordering remains
- See `evidence/verifier-pass.md`

## Fix
- Bump a generation counter per `checkVoteStatus` call and ignore stale resolutions; or skip the check while a vote is in flight

## Status
FIXED 2026-09-16 78caa32e — `voteCheckGeneration` guard + skip while voting
