# BUG-021: `createMockDye({ stainID: null })` swaps null for a random decoy, so the documented legacy 'null arm' fixture cannot be built
**Severity:** LOW · **Type:** Edge case · **Deploy unit:** test-utils · **Covered by test?** no

## Location
- `packages/test-utils/src/factories/dye.ts:181-183` — `overrides.stainID ?? randomStainId()` treats null as absent before deriving `itemID`/`id`/`name`
- `packages/test-utils/src/factories/dye.ts:39-41` — `legacyItemIdForStain(null) → 5729` exists but is unreachable from this path

## Evidence
- Reviewer row `pkg-worker-kit-test-utils-17`; no caller passes `null` today

## Fix
- `'stainID' in overrides ? overrides.stainID : randomStainId()`; add the null-arm test

## Status
FIXED 2026-09-16 98637eff — `'stainID' in overrides` honours null (itemID 5729)
