# BUG-017: oauth's env-validation 500 is returned before the security-headers middleware runs, so it carries no nosniff / no-store / HSTS
**Severity:** LOW · **Type:** Logic · **Deploy unit:** oauth · **Covered by test?** no

## Location
- `apps/oauth/src/index.ts:119-141` — returns `c.json({ error: 'Service misconfigured' }, 500)` without `next()`
- `apps/oauth/src/index.ts:145-165` — headers middleware registered after it; CLAUDE.md documents only the CORS-preflight exception

## Evidence
- Reviewer row `oauth-12`; Opus pass confirmed LOW (fires only while misconfigured, body carries nothing sensitive)

## Fix
- Register the headers middleware first (it does not depend on validated env), or set the headers inline on that one response; note the exception in CLAUDE.md if kept

## Status
FIXED (Sprint 14, `36a60081`) — the security-headers middleware is registered before env validation; the misconfiguration 500 now carries nosniff / no-store / HSTS; CLAUDE.md order paragraph updated.
