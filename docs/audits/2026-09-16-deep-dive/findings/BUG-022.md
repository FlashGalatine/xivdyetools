# BUG-022: Extractor zoom canvas never checks `MouseEvent.button`, so a right-button drag commits a pixel sample alongside the context menu
**Severity:** LOW · **Type:** Edge case · **Deploy unit:** web-app · **Covered by test?** no

## Location
- `apps/web-app/src/components/image-zoom-controller.ts:579,651-672` — mousedown/mouseup handlers gate on `isDragging` only

## Evidence
- Reviewer row `webapp-tools-b-04`

## Fix
- `if (mouseEvent.button !== 0) return;` at both handlers

## Status
OPEN
