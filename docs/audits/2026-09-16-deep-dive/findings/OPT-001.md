# OPT-001: og-worker's two SPA pass-through `fetch()` calls carry no timeout
**Impact:** LOW · **Category:** I/O · **Deploy unit:** og-worker · **Expected gain:** bounded worst-case latency on crawler requests (today: the platform's default) · **Benchmark:** wrangler dev with a stalled origin stub; measure time-to-first-byte

## Location
- `apps/og-worker/src/index.ts:516,1218`

## Evidence
- Reviewer row `og-worker-02`

## Fix
- `signal: AbortSignal.timeout(5_000)` and fall through to the static card on abort

## Status
FIXED (Sprint 10, `914c401f`) — both SPA pass-throughs carry `AbortSignal.timeout(5_000)`; a TimeoutError/AbortError falls back to the existing 302 to the app (neither site has a static card); other rejections still surface.
