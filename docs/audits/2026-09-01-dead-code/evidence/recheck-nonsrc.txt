errorResponse                      —
LocalizationService                —
createMockEnv                      apps/api-worker/tests/test-utils.ts apps/presets-api/tests/test-utils.ts 
toBannedUser                       —
InteractionResponseBody            —
autocompleteResponse               —
embedResponse                      —
infoEmbed                          —
hexToDiscordColor                  —
encodeBase64Url                    —
deleteOriginalResponse             —
sendFollowUp                       —
getLocaleInfo                      —
createTranslator                   —
getModerationHistory               —
isApiEnabled                       —
getRateLimitInfo                   —
isPresetModerationButton           —
checkBanStatus                     —
requireNotBannedCheck              —
notifyModerators                   —
validateStringLength               —
validateArray                      —
validateEnum                       —
ErrorCodeType                      —
ModerationStatus                   —
DISCORD_REQUIRED_SCOPES            —
isStateSigned                      —
findUserById                       —
findUserByDiscordId                —
findUserByXIVAuthId                —
getImageDimensions                 —
parseMultiDyeArgs                  —
withLoadingIndicator               —
isAuthorized                       —
registryCommandNames               —
deleteAsync                        —
deleteEntry                        —
