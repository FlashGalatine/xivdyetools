# Hardcoded-string (i18n) scan — Group 4

Scope: `src/components/mixer-tool.ts`, `src/components/accessibility-tool.ts`, `src/components/budget-tool.ts`, `src/components/market-board.ts`, `src/services/market-board-service.ts`, `src/services/world-service.ts`, `src/services/mixer-blending-engine.ts`, `src/services/pricing-mixin.ts` (branch `monorepo-2.0-prep`, read-only). All paths relative to `c:\dev\XIVProjects\xivdyetools\apps\web-app\`.

| File:Line | String (verbatim, truncated to 80 chars) | Context | Priority | Existing key? | Suggested key |
|---|---|---|---|---|---|
| src/components/budget-tool.ts:781 | `` `~${price.board.toLocaleString()} gil` `` | currency — target-dye card price line; `gilWord = LanguageService.getCurrency('Gil')` already exists in the same file (L1238) | High | `LanguageService.getCurrency('Gil')` (core) | use `getCurrency('Gil')` |
| src/components/budget-tool.ts:1516 | `` `${groupGil.toLocaleString()} gil` `` | currency — tier-group header price (coffer tier, single price) | High | `getCurrency('Gil')` | use `getCurrency('Gil')` |
| src/components/budget-tool.ts:1518 | `` `${groupGil.toLocaleString()} – ${gilMax!.toLocaleString()} gil` `` | currency — tier-group header price range (coffer tier) | High | `getCurrency('Gil')` | use `getCurrency('Gil')` |
| src/components/budget-tool.ts:1526 | `` `${tierMeta.price.toLocaleString()} gil` `` | currency — Standard Spectrum tier header price | High | `getCurrency('Gil')` | use `getCurrency('Gil')` |
| src/components/budget-tool.ts:1539 | `` `${boardPrice.toLocaleString()} gil` `` | currency — Wide Spectrum tier header board price | High | `getCurrency('Gil')` | use `getCurrency('Gil')` |
| src/components/budget-tool.ts:1523 | `` `${tierMeta.price.toLocaleString()} ${tierMeta.currency}` `` | currency — raw core currency string (`'Gil'` / `"Skybuilders' Scrips"`) rendered as tier-header sub-line / main price (L1530, L1540, L1542); the sibling `priceOf()` at L464 already wraps it in `getCurrency()` | High | `LanguageService.getCurrency(meta.currency)` (pattern at L464) | use `getCurrency(tierMeta.currency)` |
| src/components/mixer-tool.ts:2205 | `` `${price.currentMinPrice.toLocaleString()} gil` `` | currency — price shown next to hex in the selected-dye cards (desktop + drawer) | High | `getCurrency('Gil')` | use `getCurrency('Gil')` |
| src/components/mixer-tool.ts:1920 | `` `Blend: ${this.mixingMode} @ ${Math.round(this.mixRatio * 100)}%` `` | text — export-sheet `meta` line; reaches the copied/downloaded text (palette-export.ts L122) and JSON (L203). "Blend:" is English and `mixingMode` is the raw id (`ryb`, `oklab`…) rather than the `mixer.model*` / `config.mixing*` label | High | `mixer.blend` = "Blend" (label only, not the sentence); model labels at `mixer.modelRyb` etc. | `mixer.exportBlendMeta` = "Blend: {model} @ {ratio}%" via `tInterpolate`, model via `t('mixer.model'+…)` |
| src/components/mixer-tool.ts:853 | `` name: `Custom (${hex})` `` | dye.name — virtual custom-colour dye; `dyeName()`/`getDyeName(negativeId) \|\| dye.name` falls through to this English name in slot labels (L1670), selected-dye cards (L410), saved-mix collection name (L1384) | High | `colorPalette.customColor` = "Custom Color" (close, not identical) | `common.customColorName` = "Custom ({hex})" via `tInterpolate` |
| src/components/accessibility-tool.ts:418 | `` name: `Custom (${hex.toUpperCase()})` `` | dye.name — virtual dye name shown in selected-dye list (L650/L1724), lens grid label (L1455 via `dyeResults[i].dyeName`), pair rows (L1537), result cards | High | `colorPalette.customColor` (close) | `common.customColorName` |
| src/components/budget-tool.ts:1877 | `` name: `Custom (${hex.toUpperCase()})` `` | dye.name — virtual target; shown on target card (L763), verdict headline `{t}` interpolation (L1256/1263/1270), result card | High | `colorPalette.customColor` (close) | `common.customColorName` |
| src/components/accessibility-tool.ts:121 | `'~6% males'` | text — `VISION_TYPES.prevalence`, rendered verbatim under the Deuteranopia checkbox (L725, drawer L1820) and in the lens tab sub-row (L1383) | High | none (no prevalence/males key) | `accessibility.prevalenceDeuteranopia` = "~6% males" (or `accessibility.prevalenceMales` = "~{pct} males") |
| src/components/accessibility-tool.ts:127 | `'~2% males'` | text — same three render sites for Protanopia | High | none | `accessibility.prevalenceProtanopia` |
| src/components/accessibility-tool.ts:115,133,139 | `'~92%'`, `'~0.01%'`, `'~0.003%'` | text — remaining `prevalence` values; digits + `~%` only, but they share the constant with the two "males" strings and would move together; "%" placement differs in some locales | Low | none | `accessibility.prevalence{Normal,Tritanopia,Achromatopsia}` |
| src/components/budget-tool.ts:1881-1882 | `category: 'Custom'`, `acquisition: 'Custom'` | dye.name-style — virtual target's acquisition is rendered on the target `v4-result-card` when `showAcquisition` is on (result-card L1609 calls `getAcquisition('Custom')`, which has no localized entry → falls back to English "Custom") | Medium | none for "Custom" acquisition | `common.custom` or handle in `getAcquisition` fallback |
| src/components/mixer-tool.ts:857-858 | `category: 'Custom'`, `acquisition: 'Custom'` | same virtual-dye fields; mixer never renders the input dyes as cards, so only reachable through the export sheet (`dye` entries L1906-1907) | Low | none | as above |
| src/components/accessibility-tool.ts:422-423 | `category: 'Custom'`, `acquisition: 'Custom'` | same; accessibility cards set `showAcquisition = false` (L1163) so not rendered today | Low | none | as above |
| src/components/budget-tool.ts:1529 | `` `${LanguageService.t('budget.boardWord')} ${boardPrice.toLocaleString()}` `` | concat — "board 1,234" built from a single translated word + number; word order / particle placement (ja/ko) cannot be expressed | Medium | `budget.boardWord` = "board" | `budget.boardPrice` = "board {price}" via `tInterpolate` |
| src/components/budget-tool.ts:1540 | `` `${LanguageService.t('budget.orWord')} ${localCost}` `` | concat — "or 100 Skybuilders' Scrips" from translated "or" + value | Medium | `budget.orWord` = "or" | `budget.orLocalCost` = "or {cost}" via `tInterpolate` |
| src/components/mixer-tool.ts:1236 | `` `${LanguageService.t('mixer.spread')} ${spread.toFixed(dp)}` `` | concat — label + number in one text node ("Model spread 4.2"); harmless in most locales but no separator/colon control per language | Low | `mixer.spread` = "Model spread" | `mixer.spreadValue` = "{label} {value}" or keep as two spans |
| src/components/budget-tool.ts:121-124 | `'STANDARD'`, `'WIDE #1'`, `'WIDE #2'`, `'COFFER'` | text — `TIER_META.tag`, rendered as the mono tier badge in every ledger group header (L1472). Source comment says "identifiers, never localized" (design decision) but they are English words visible to every user | Low | `colorPalette.spectrumStandard` / `spectrumWide1` / `spectrumWide2` = "Standard Spectrum" / "Wide Spectrum #1" / "#2"; `filters.excludeCoffers` (not a tag) | `budget.tierTagA/B/C/X` — or keep as deliberate mono tags and document |
| src/components/mixer-tool.ts:1198 | `spectral: 'Spectral'` | text — `MODEL_SHORT` row header in the mixing field (L1281). Others (RYB/OKLAB/LAB/HSL/RGB) are technical abbreviations; "Spectral" is an English word, and its own `title` tooltip resolves to `mixer.modelSpectral` = "Pigment" so header and tooltip disagree | Low | `mixer.modelSpectral` = "Pigment" | use `t('mixer.modelSpectral')` or a short key `mixer.modelSpectralShort` |
| src/components/accessibility-tool.ts:1547,1574-1580 | `` `${…} NRM \u2192 ${…} ${activeShort}` `` with `NRM/DEU/PRO/TRI/ACH` | text — pair-row note built from English-derived mono short codes (Normal/Deuteranopia/…); plus concat of two formatted values + codes | Low | `getVisionType(...)` gives full localized names only | `accessibility.visionShort{Normal,…}` or reuse localized names |
| src/components/market-board.ts:250 | `` optgroup.label = `${dc.name} (${dc.region})` `` | text — `region` comes from `public/json/data-centers.json`: "Europe", "Japan", "North-America", "Oceania", "中国", "한국" — a mix of English and native labels shown to every locale | Low | none | `marketBoard.region{Europe,Japan,NorthAmerica,Oceania,China,Korea}` keyed by region id |
| src/components/budget-tool.ts:870 | `this.dyeName(dye).split(' ')[0]` | text (locale-unsafe truncation) — quick-pick label shows the first space-separated word of the localized name; fine for en/de/fr, shows the whole name for ja/ko/zh (no spaces) so the 3-col grid overflows/ellipsizes differently per locale | Low | n/a | use CSS ellipsis on the full localized name instead of word-splitting |
| src/components/budget-tool.ts (16 sites) | `.toLocaleString()` without locale | number formatting per BROWSER locale, not the app's chosen locale — L464, 781, 879, 1258, 1265, 1279, 1283, 1516, 1518, 1523, 1526, 1529, 1533, 1539, 1732, 1742 | Low | n/a | `toLocaleString(LanguageService.getCurrentLocale())` or a shared `formatNumber()` |
| src/components/mixer-tool.ts (1 site) | `.toLocaleString()` without locale | L2205 (price) | Low | n/a | as above |

## Verified-and-skipped (not findings)

- `LanguageService.getDyeName(dye.itemID) || dye.name` (mixer L410/1150/1670, accessibility L650/1724/1911/1990/1993, budget L506) — localized getter with English fallback; only the *virtual* "Custom (…)" dyes actually hit the fallback (listed above).
- accessibility-tool `VISION_TYPES[].description` ("Red-Green Colorblindness" etc., L116-140) — **never rendered**; tab tooltips use `t('accessibility.visionDesc…')`. Dead data, not a user-visible string.
- mixer `title="${MODEL_SHORT} · ${r}/${100-r} · ${hex}"` (L1316), `ΔE x.x` badge (accessibility L1446), `≤ n` (budget L927), `×n` (budget L1603), `n / m` counters, `—` dashes, `+`/`→`/`?`/`ⓘ` glyphs — technical/symbol-only.
- budget L1151/1156/1805-1814 `RouterService.navigateTo(..., { dye: dye.name })` — URL parameter, not displayed.
- world-service.ts L504 `throw new Error('Failed to load server data…')` — caught in the same method and only logged.
- market-board.ts L255 `` `${dc.name} - ${t('marketBoard.allWorlds')}` `` — proper noun + translated phrase joined by a dash; acceptable.
- All `logger.*` strings, storage keys, CSS strings, data-testid, hex ramps.

## Summary

27 table rows across 4 files. **High: 13** (budget-tool 7 — five hardcoded " gil" suffixes, one raw `tierMeta.currency`, one "Custom (…)" virtual name; mixer-tool 4 — " gil", the `Blend:` export meta line, "Custom (…)"; accessibility-tool 2 — "~6% males" / "~2% males" prevalence labels). **Medium: 3** (budget-tool: virtual target `acquisition: 'Custom'` reaching the result card, and the two `boardWord`/`orWord` + value concatenations). **Low: 11** (mixer 4: spread label concat, "Spectral" row header vs "Pigment" tooltip, Custom category/acquisition fields, toLocaleString; accessibility 3: number-only prevalence strings, NRM/DEU/… short codes, Custom category/acquisition; budget 3: STANDARD/WIDE/COFFER tier tags (deliberate per comment), `split(' ')[0]` quick-pick truncation, 16 locale-less `toLocaleString()`; market-board 1: English/native-mixed `dc.region` optgroup label). Per file: budget-tool.ts 13 rows (7 High / 3 Medium / 3 Low), mixer-tool.ts 8 rows (4 High / 0 / 4 Low), accessibility-tool.ts 5 rows (2 High / 0 / 3 Low), market-board.ts 1 row (Low). The single most valuable fix is replacing the five literal `gil` suffixes and the raw `tierMeta.currency` in budget-tool (plus mixer L2205) with the already-imported `LanguageService.getCurrency(...)` — the file already does this correctly at L464 and L1238, so it's an internal inconsistency rather than a missing facility. **Completely clean files (no user-visible hardcoded strings): `src/services/market-board-service.ts`, `src/services/world-service.ts`, `src/services/mixer-blending-engine.ts`, `src/services/pricing-mixin.ts`.**
