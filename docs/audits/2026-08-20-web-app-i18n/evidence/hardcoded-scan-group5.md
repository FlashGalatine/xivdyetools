# Hardcoded-string (i18n) scan — Group 5 (v4 shell / chrome components)

Scope: `apps/web-app/src/components/v4/{config-sidebar,result-card,dye-palette-drawer,v4-layout-shell,v4-app-header,display-options-v4,dye-filters-v4,share-button,range-slider-v4,toggle-switch-v4,language-modal,theme-modal,base-lit-component}.ts` + `src/components/v4-layout.ts`. Read-only; every file read end to end. Branch `monorepo-2.0-prep`.

| File:Line | String (verbatim, ≤80 chars) | Context | Priority | Existing key? | Suggested key |
|---|---|---|---|---|---|
| v4/dye-palette-drawer.ts:1327 (+ :33-62) | `${LanguageService.t(CATEGORY_TRANSLATION_KEYS[category] \|\| category)}` → renders "Blues", "Browns", "Greens", "Neutral", "Purples", "Reds", "Special", "Yellows" | text — `CATEGORY_TRANSLATION_KEYS` / `DYE_CATEGORY_ORDER` are keyed White/Grey/Black/Brown/Red/… (singular) but runtime `dye.category` is Blues/Browns/Greens/Neutral/Purples/Reds/Special/Yellows, so the lookup never hits; `t('Blues')` falls through to "return key" (and logs `Translation not found` per heading per render). Every category heading in the palette drawer is English in all 6 locales. | High | core `LanguageService.getCategory(category)` already translates these (core `data/locales/*.json` `categories.Blues` = "Blau"/"Bleus"/…); web keys `colorPalette.blues/browns/greens/purples/reds/yellows` exist but have no Neutral/Special | Replace with `LanguageService.getCategory(category)`; drop the dead `CATEGORY_TRANSLATION_KEYS` / fix `DYE_CATEGORY_ORDER` to the real category names |
| v4/dye-palette-drawer.ts:904 | `tInterpolate('colorPalette.randomDyeSelected', { name: randomDye.name })` | toast / dye.name — English dye name interpolated into a localized toast | High | key `colorPalette.randomDyeSelected` exists; name should be `LanguageService.getDyeName(randomDye.id) \|\| randomDye.name` (the file's own `getLocalizedDyeName` helper at :1210) | — (use existing getter) |
| v4/dye-palette-drawer.ts:797 | `dyes.filter((d) => d.name.toLowerCase().includes(query))` | dye.name — search matches English names only; a ja/de/fr/ko/zh user typing the localized name gets "No dyes found" | Medium | — | Also test `LanguageService.getDyeName(d.id)` in the filter |
| v4/dye-palette-drawer.ts:1238-1239 | `` `${t('aria.removeFromFavorites')}: ${localizedName}` `` / `addToFavorites` | concat (aria-label; colon punctuation differs in fr/ja/zh) | Low | `aria.removeFromFavorites`, `aria.addToFavorites` exist | `aria.removeFromFavoritesNamed` / `aria.addToFavoritesNamed` with `{name}` |
| v4/dye-palette-drawer.ts:1271 | `${t('colorPalette.favorites')} (${this.favoriteDyes.length})` | concat (count in parens) | Low | `colorPalette.favorites` | `colorPalette.favoritesCount` "{label} ({count})" via tInterpolate |
| v4/result-card.ts:1013 | `` `${price.toLocaleString()} G` `` | currency — hardcoded "G" suffix on the market price (vendor cost right below it at :1022 correctly uses `getCurrency`) | High | `LanguageService.getCurrency('Gil')` (core) | Use `getCurrency('Gil')` (or a `common.gilShort` key if the abbreviation is wanted) |
| v4/result-card.ts:1535 | `<div class="metric-label">STAIN</div>` | text — verdict readout label; its sibling "HUE OFF" uses `t('resultCard.hueOff')` | High | similar: `config.showStain` = "Stain ID" (toggle label, not the same surface) | `resultCard.stainShort` |
| v4/result-card.ts:1698-1699, 1706-1707 | `${t('common.replace')} ${t('common.slot')} 1` / `… 2` | concat — two translated fragments + number joined into one sentence (word order: ja/ko put the verb last) | High | `mixer.replaceSlot` = "Replace Slot", `mixer.replacedSlot1/2` exist | `resultCard.replaceSlotN` "Replace Slot {n}" via tInterpolate |
| v4/result-card.ts:1484 | `aria-label="Dye result: ${dyeName}"` | attr (aria-label on the card `<article>`) | Medium | — | `aria.dyeResult` "Dye result: {name}" via tInterpolate |
| v4/result-card.ts:210 | `primaryActionLabel: string = LanguageService.t('common.selectDye')` | stale-cache — field initialiser runs once per element; ResultCard never calls `LanguageService.subscribe`, so a card that survives a locale switch keeps the old button label AND all its other `t()` strings (market label, hueOff, menu items) until the parent recreates it | Medium | `common.selectDye` | Resolve the default in `render()` (`this.primaryActionLabel ?? t(...)`) and subscribe in connectedCallback |
| v4/result-card.ts:1276 | `` `${t('common.slot')} ${i + 1}` `` | concat (slot-picker modal labels) | Low | `common.slot` | `common.slotN` "Slot {n}" |
| v4/result-card.ts:1385 | `t('resultCard.addingDye') + ':'` | concat — trailing colon appended in code | Low | `resultCard.addingDye` | Fold the colon into the key value (or `resultCard.addingDyeLabel`) |
| v4/result-card.ts:1298 | `'Unknown'` | text — slot name fallback in the replace-slot modal when the stored dye ID no longer resolves | Low | — (`comparison.costUnknown` is a sentence, not reusable) | `common.unknown` |
| v4/result-card.ts:1468 | `<div class="ticket">No data</div>` | text — rendered when `.data` is unset (debug-ish, but visible) | Low | — | `resultCard.noData` |
| v4/result-card.ts:1013, 1022 | `toLocaleString()` ×2 | no locale argument (uses browser locale, not the app locale) | Low | — | `toLocaleString(LanguageService.getCurrentLocale())` |
| v4-layout.ts:485 | `<p …>Loading ${toolId}...</p>` | text — tool-switch loading state, shows the raw router id ("Loading extractor...") | High | `presets.loading` = "Loading presets...", `loading.title` = "Loading..." (line 1120) | `common.loadingTool` "Loading {tool}…" with `tools.<id>.title`, or reuse `loading.title` |
| v4-layout.ts:635 | `Failed to load tool` | error (innerHTML error panel when a dynamic import fails) | High | similar: `preset.imageLoadFailed`, `*Failed. Please try again.` | `errors.toolLoadFailed` |
| v4-layout.ts:636 | `Please try again or refresh the page` | error | High | similar: `market.refreshError` "…Please try again.", `preset.tryAgain` "Try Again" | `errors.tryAgainOrRefresh` |
| v4-layout.ts:651 | `${toolId} Tool` | text — `renderPlaceholder` for an unknown ToolId (default branch; RouterService validates ids so effectively unreachable) | Low | — | `common.comingSoonTool` "{tool} Tool" |
| v4-layout.ts:652 | `Coming soon` | text — same placeholder | Low | — | `common.comingSoon` |
| v4/config-sidebar.ts:1467 | `RGB DIST - ${t('config.matchingRgb')}` | option label — "DIST" is an English abbreviation used as the method tag (the other tags ΔE2000/ΔEOK/ΔE76/REDMEAN are technical ids) | Medium | `comparison.mRgbShort` = "RGB distance", `config.matchingRgb` = "Basic" | `config.matchingRgbTag` (or drop the tag and let the key carry it) |
| v4/config-sidebar.ts:1469 | `DISTINGUISH % - ${t('config.matchingDistinguish')}` | option label — English word as method tag | Medium | `accessibility.unitPctShort` / `comparison.mDistinguishShort` = "Distinguishability %" | `config.matchingDistinguishTag` |
| v4/config-sidebar.ts:1613 | `user.global_name \|\| user.username \|\| 'User'` | text — account-card display-name fallback (edge: both names missing) | Low | — | `config.userFallback` |
| v4/config-sidebar.ts:1579-1580 | `${t('preset.cfgStoredLocally')} — ${t('preset.cfgStoredLocallyDesc')}` | concat — two translated fragments joined with an em dash into one hint line | Low | both keys exist | Merge into one key `preset.cfgStoredLocallyHint` |
| v4/config-sidebar.ts:1103 | `` ` (${t('config.singlePixel')})` `` appended to `1×1` | concat (parenthesised suffix) | Low | `config.singlePixel` | `config.sampleAreaSingle` "1×1 ({label})" — or accept as-is |
| v4/config-sidebar.ts:1947 | `<optgroup label="${dc.name} (${dc.region})">` | text — `region` comes from `/json/data-centers.json` ("North-America", "Europe", "Japan", "Oceania"); English in every locale | Low | — | `market.region.<slug>` lookup, or localize the JSON |
| v4/config-sidebar.ts:69 | `first.toLocaleUpperCase()` ×1 | no locale argument (avatar initial) | Low | — | pass `LanguageService.getCurrentLocale()` |
| v4/v4-layout-shell.ts:955, 993, 998, 1014-1015 | `t('aria.closePalette')`, `t('colorPalette.mobileHint')`, `t('aria.dismissHint')`, `t('aria.showColorPalette')` | stale-cache — shell never subscribes to LanguageService; these only refresh when some other @state flips (mobile hint text is visible UI) | Medium | keys exist | Add a `LanguageService.subscribe(() => this.requestUpdate())` like config-sidebar/app-header |
| v4/display-options-v4.ts:382-482 | `t('config.colorFormats')`, `t('config.hexCodes')`, … `t('config.showSpectrum')` | stale-cache — component does not subscribe, and the parent's re-render passes the same boolean props so Lit skips the child update; all group headers + toggle labels stay in the old language until a toggle flips or the element is re-mounted | Medium | keys exist | Subscribe in connectedCallback (or have config-sidebar pass a `locale` prop) |
| v4/dye-filters-v4.ts:264-352 | `t('filters.dyeTypes')`, `t('filters.excludeMetallic')`, … | stale-cache — same mechanism as display-options-v4 | Medium | keys exist | Same fix |
| v4/share-button.ts:323, 328 | `t('share.copied') \|\| 'Copied!'`, `t('share.button') \|\| 'Share'` | text — English fallbacks; dead in practice (`t()` returns the key, never falsy) but the literal is the visible label if the key were ever removed; component also never subscribes to locale changes | Low | `share.copied`, `share.button` exist | Drop the `\|\|` fallbacks |
| v4/language-modal.ts:158 | `englishName.textContent = locale.englishName` → "Japanese", "German", "French", "Korean", "Chinese" | text — English exonym rendered under the native name regardless of the UI language (a German UI shows "Japanese", not "Japanisch"); native names (:150) are correct by design and skipped | Low | — | `languages.<code>` per-locale exonym, or drop the subtitle |

Not listed (verified not user-visible): `config-sidebar.ts:740` thrown `Failed to load server data…` is caught and goes to `logger.error` only; `share-button.ts:284/312` `setError('Invalid share params…' / 'Failed to share')` land in `BaseLitComponent.errorMessage`, which no template in this group renders. Brand names (`Universalis`, `GarlandTools`, `TeamCraft`, `Saddlebag Exchange`, `XIVAuth`/`Discord`, `XIV Dye Tools`), technical tags (HEX/RGB/HSV/LAB/CMYK/ΔE2000/OKLCH/RYB/REDMEAN), symbols (×, ▼/▲, ▾, ✓, —, ·, +) and native language names were skipped per the brief.

Incidental (out of scope, surfaced by a grep while checking `primaryActionLabel`): `src/components/swatch-tool.ts:2331` sets `card.setAttribute('primary-action-label', 'Explore Dye')` — a hardcoded English card button label.

## Summary

31 rows across 9 of the 14 files. By priority: **High 9** (palette-drawer category headings English in every locale via a never-matching key map [1 row covering all 8 headings], random-dye toast uses English `dye.name`, result-card hardcoded "G" gil suffix, result-card "STAIN" label, result-card "Replace Slot 1/2" concatenation, v4-layout "Loading {toolId}..." / "Failed to load tool" / "Please try again or refresh the page"), **Medium 8** (aria "Dye result:", result-card no-subscribe + construction-time `primaryActionLabel`, palette search matches English names only, `RGB DIST` / `DISTINGUISH %` option tags, stale-cache in v4-layout-shell / display-options-v4 / dye-filters-v4), **Low 14** (concat/punctuation joins, 'Unknown'/'No data'/'User'/'Coming soon' fallbacks, data-center region strings, share-button dead English fallbacks, language-modal English exonyms, and 3 `toLocaleString`/`toLocaleUpperCase` calls without a locale). Per file: dye-palette-drawer 5, result-card 10, v4-layout 5, config-sidebar 7, v4-layout-shell 1, display-options-v4 1, dye-filters-v4 1, share-button 1, language-modal 1. **Completely clean:** `v4-app-header.ts`, `range-slider-v4.ts`, `toggle-switch-v4.ts`, `theme-modal.ts`, `base-lit-component.ts` (all user-facing strings go through `LanguageService.t`/getters, and app-header subscribes to locale changes).
