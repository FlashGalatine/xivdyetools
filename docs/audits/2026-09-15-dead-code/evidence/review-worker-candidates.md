# Bounded worker candidate review

Read-only `git grep` over tracked `apps/**`, `packages/**`, `scripts/**`, and configs; unit CLAUDE files read. Syntax leads below were manually opened. No source changed.

| Candidate | Location | Verdict and removal scope | Tests / caveat |
|---|---|---|---|
| Unbatched preset-status wrappers | `apps/moderation-worker/src/services/ban-service.ts:631-649` | **REMOVE, HIGH, ~18 source lines.** `banUser`/`unbanUser` batch `hideUserPresetsStatement`/`restoreUserPresetsStatement` with audit-log statements; wrappers have zero production callers and create an unaudited mutation route. | Remove the two direct-wrapper describe blocks, `ban-service.test.ts:662-730` (~69 test lines). Preserve statement/batched ban-unban tests. |
| Fetch log wrappers | `apps/moderation-worker/src/utils/url-sanitizer.ts:233-285` | **REMOVE, HIGH, ~31 source lines.** Zero production callers; only their own tests. | Remove `sanitizeFetchRequest`/`sanitizeFetchResponse` test blocks, `url-sanitizer.test.ts:337-460` (~124 lines). |
| Header sanitization cascade | `apps/moderation-worker/src/utils/url-sanitizer.ts:135-162` | **REMOVE WITH the fetch wrappers, HIGH, ~28 source lines.** Its only source callers are the two removable wrappers; docs/examples are not runtime consumers. | Then remove `sanitizeHeaders` tests, `url-sanitizer.test.ts:154-333` (~180 lines). Preserve `sanitizeUrl` and `sanitizeErrorMessage` tests/paths. |
| `Translator.getMeta` | `apps/moderation-worker/src/services/bot-i18n.ts:203-205` | **REMOVE, HIGH, 3 source lines.** Explicit `@testonly`, no handler/source caller; unlike `@testonly` itself, the zero-reference result supports removal. | Remove `bot-i18n.test.ts:209-232` (~24 lines); retain locale resolution and translator-string tests. Do not alter `LocaleData.meta`. |
| `getPreference` | `apps/discord-worker/src/services/preferences.ts:156-166` | **REMOVE, HIGH, ~11 source lines.** Directly test-only; no production key-by-key read, while live callers use whole-preference access. | Remove `preferences.exhaustive.test.ts:338-360` (~23 lines); preserve `getUserPreferences` tests and defaults. |
| Unicode truncation helper | `apps/presets-api/src/services/moderation-service.ts:53-62` | **REMOVE, HIGH, ~10 source lines.** No production caller; current moderation pipeline neither truncates before storage nor logging. | Remove `moderation-service.test.ts:101-139` (~39 lines); preserve profanity filter tests. |
| `duplicateResponse` | `apps/presets-api/src/utils/api-response.ts:174-176` | **REMOVE, HIGH, 3 source lines.** Four production duplicate 409s construct equivalent responses inline; no caller imports helper. | Remove `api-response.test.ts:131-135` (~5 lines); preserve `errorResponse` and other status helpers. |
| `VoteRow` | `apps/presets-api/src/types.ts:188-192` | **REMOVE, HIGH, 5 source lines.** Test-only structural type; no D1 query/type consumer. | Remove `types.test.ts:318-326` (~9 lines) and import. Preserve adjacent DB row types. |

Rejected: `sanitizeHeaders` is not independently live, but must not be removed ahead of the two fetch-wrapper callers; `hideUserPresets`/`restoreUserPresets` tests do not observe the batched production behavior, so they do not justify retaining the unsafe wrappers. Remaining syntax-survey rows in these units were not part of this bounded review.
