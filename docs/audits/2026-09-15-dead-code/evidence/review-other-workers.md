# Other-worker high-signal review

Read-only, bounded candidate survey over tracked source, tests, `package.json`, `wrangler.toml`, scripts, and docs-theme imports. It covers 136 tracked TS files across these five units; it is not a full member-by-member audit. No gates or source edits ran.

| Unit | Entry/reachability coverage | Verdicts and limitations |
|---|---|---|
| api-worker | `wrangler.toml` → `src/index.ts`; Universalis router imports `CACHE_CONFIGS`; docs VitePress has 18 tracked files and `.vue` components import the apparent-zero-reference `theme/lib/*` helpers. `scripts/build-item-names.mjs` is documented/manual and emits tracked ko/zh data. | **REMOVE/MOVE candidate:** `CacheConfigKey`, `src/universalis/config/cache.ts:49` (1 type line), has only `cache.test.ts:6,120` use. Inline `keyof typeof CACHE_CONFIGS` in that test or make the test-local alias; preserve `CACHE_CONFIGS` and router imports. Do not file Vue helpers or development/test limiter selectors. |
| image-worker | Binding-only worker entry is `src/index.ts`; `workers_dev=false` and no routes in both envs, with discord/presets service-binding consumers. Photon/validator survey leads have same-file or test callers. | **Type-hygiene candidate:** optional `Env.ENVIRONMENT`, `src/types.ts:12` (1 line), is unset in `wrangler.toml` and logger uses `readEnvironmentFromEnv:false`; remove it and two test fixture fields if no shared `Env` contract needs it. This is not runtime dead-path evidence. |
| oauth | `wrangler.toml` entry and auth routes/services were checked; rate-limit KV fallback is deliberately reachable in development/tests. | **KEEP:** deprecated re-export block in `src/types.ts:13-41`; migration compatibility surface is consumed and cannot be judged from local syntax alone. Do not remove KV fallback or `resetRateLimiter` isolation hook. |
| og-worker | `wrangler.toml` beta/production entries point to `src/index.ts`; generator/translator/SVG helpers are route-dispatched, while font subsetting script is a documented build asset. | No removal candidate confirmed. Survey's apparent test-only OG data/strings are used by route selection or generated cards; do not treat no direct import as unreachable. |
| stoat-worker | Node entry/config/router checked. It is parked; tests and direct imports cover parser/help/mocks. | **KEEP/P3:** `withLoadingIndicator`, `DYE_INFO_REACTIONS`, `parseMultiDyeArgs`, and related reaction scaffolding have no current production caller but CLAUDE documents the planned reaction/listener work. Revisit only if Stoat is resumed or retired; do not prune while parked. |

Additional rejected leads: api `clearRateLimits` and RequestCoalescer observation methods are explicit test-isolation/observation hooks; OAuth re-exports and rate-limit fallback are compatibility/development paths; image's service-binding-only configuration is an external entrypoint rather than an orphan. Dynamic Cloudflare routes, VitePress `.vue` imports, and external consumers limit static conclusions.
