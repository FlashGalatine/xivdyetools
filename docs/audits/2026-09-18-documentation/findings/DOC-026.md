# DOC-026: `versions.md` says auth 2.0.2 still needs publishing (it is published) and names none of the four packages that do
**Severity:** MEDIUM · **Kind:** STALE-VERSION · **Deploy unit:** repo · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/versions.md:10`

## Evidence
- `evidence/npm-versions.txt` (npm registry API, 2026-09-18): auth 2.0.2 = local. Ahead of npm: logger 2.2.1 / 2.2.0, worker-kit 1.4.0 / 1.3.0, core 5.3.0 / 5.2.0, bot-logic 4.3.0 / 4.2.0.
- The version tables themselves pass `pnpm docs:check-versions`; only this prose is wrong. The unpublished four are release drift (see the report), not a defect.
- Reviewer candidates: ES-05 (verdicts in `../evidence/verify-*.md`).

## Fix
- Replace the sentence with the four pending packages — or drop npm state from the page, since no gate can keep it true.

## Status
FIXED 2026-09-18 `b9428956` (PR #190, draft — not merged). The callout was the visible part: 22 "(prepared)" markers on released rows were stale too. 15 are removed here; the 7 in the discord-worker and api-worker tables are removed by PR #189 and PR #191, which insert rows into those tables (`da661eb0` explains the split).
