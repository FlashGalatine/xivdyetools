# DOC-015: `api-contracts.md` says presets-api writes five `moderation_log` actions; it writes six
**Severity:** MEDIUM · **Kind:** MISSING · **Deploy unit:** presets-api · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/architecture/api-contracts.md:475-476` — "`revert` is the fifth action this API writes"

## Evidence
- `apps/presets-api/src/handlers/moderation.ts:436-453` `getActionFromStatusChange` returns `requeue` for a move back to `pending`. `docs/projects/presets-api/database.md:80` and `endpoints.md:278` already list it.
- Reviewer candidates: AMR-02 (verdicts in `../evidence/verify-*.md`).

## Fix
- Add `requeue`; say six.

## Status
OPEN
