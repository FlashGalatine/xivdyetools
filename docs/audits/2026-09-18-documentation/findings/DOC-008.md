# DOC-008: `/manual` describes `/mixer` as one blend; it is a five-ratio sweep
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:63` — `manual.mixer.description`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `packages/bot-logic/src/commands/mixer.ts:56` `MIXER_SWEEP_RATIOS` = 25/40/50/65/80; `:113-124` matches a dye per ratio and flags the best.
- Reviewer candidates: MAN-07 (verdicts in `../evidence/verify-*.md`).

## Fix
- Describe the five-ratio sweep and the per-ratio match.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
OPEN
