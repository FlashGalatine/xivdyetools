# DOC-005: `/manual` says it lists "all available commands" and names 9 of 17
**Severity:** MEDIUM · **Kind:** MISSING · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `apps/discord-worker/src/handlers/commands/manual.ts:121-239` — `buildEmbeds()`
- `packages/bot-logic/src/i18n/locales/en.json:47` — "all available commands organized by category"

## Evidence
- `src/commands/registry.ts:41-58` registers `/comparison`, `/contrast`, `/accessibility`, `/budget`, `/preset`, `/changelog`, `/stats`; none is named. Only `/a11y` has a recorded reason (`registry.ts:45-46`, shares the accessibility handler).
- `evidence/manual-check-served.txt`: busiest embed uses 5 of 25 fields; largest reply 3,266 / 6,000 characters (fr).
- Reviewer candidates: MAN-08 (verdicts in `../evidence/verify-*.md`).

## Fix
- Add the seven commands, grouped by the registry's own categories; name `/a11y` beside `/accessibility`. Guard with a roster test.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIXED — PR #189 (`29e055e9`, `45051233`, review fixes `e94f8fd4`), merged 2026-09-19 as `53090f68`; the discord-worker production deploy that merge triggered succeeded.
