# DOC-010: `/manual` documents a `/preferences set <key> <value>` shape that does not exist
**Severity:** LOW · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:74` — `manual.preferences.name`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `schemas.ts:628-757`: `set` takes 15 named options, no key/value pair; `:791-862` the `filters set|show|reset` group is never mentioned.
- Reviewer candidates: MAN-05 (verdicts in `../evidence/verify-*.md`).

## Fix
- Syntax `/preferences show` | `set [option…]` | `reset [key]` | `filters set|show|reset`.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`, `45051233`, review fixes `e94f8fd4`), not merged. Becomes FIXED on merge + the discord-worker deploy it triggers.
