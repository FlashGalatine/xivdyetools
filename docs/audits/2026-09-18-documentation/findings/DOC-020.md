# DOC-020: `web-app/overview.md` says every backend enforces an origin allowlist; api-worker allows `*`
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** web-app · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/projects/web-app/overview.md:242` (contradicted by the page's own table at `:250`)

## Evidence
- `apps/api-worker/src/index.ts:100-103` `cors({ origin: "*" })`, commented "permissive for public read-only API". oauth and presets-api do allowlist.
- Reviewer candidates: WAP-03 (verdicts in `../evidence/verify-*.md`).

## Fix
- "oauth and presets-api enforce an origin allowlist; api-worker is public and allows any origin."

## Status
FIXED 2026-09-18 `589846b0` (PR #190, draft — not merged). Refined in verification: `POST /v1/telemetry` keeps its own exact-Origin allowlist.
