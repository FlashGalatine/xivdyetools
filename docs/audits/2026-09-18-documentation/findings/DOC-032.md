# DOC-032: api-worker `endpoints.md`: two error codes missing from the reference table, two parameter caps undocumented
**Severity:** LOW · **Kind:** MISSING · **Deploy unit:** api-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/projects/api-worker/endpoints.md:494-508` (error table), `:48` (`page`), `:153` (`q`)

## Evidence
- `apps/api-worker/src/lib/api-error.ts:27,29` `INVALID_COLOR_WHEEL`, `INVALID_HARMONY_TYPE` (named only inline at `:383,:395`). `src/routes/dyes.ts:47,289` `page` ≤ 1000; `:45,57-63` `q` ≤ 100 characters — both 400 past the cap.
- Reviewer candidates: API-01, API-02, API-03 (verdicts in `../evidence/verify-*.md`).

## Fix
- Add two table rows and the two caps. The public docs site (`apps/api-worker/docs/`) was out of scope — check it for the same gaps.

## Status
FIXED 2026-09-18 `4b912720` (PR #190, draft — not merged). The public developer site had the `page` / `q` gaps too: fixed in draft PR #191 (`2a87d3e0`, api-worker 0.14.3).
