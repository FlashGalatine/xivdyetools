# DOC-003: `/manual` documents the deleted 4.x `/swatch color|grid` instead of the `.chara` command
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:70-71` — `manual.swatch.name` / `.description`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `apps/discord-worker/src/commands/schemas.ts:870-901`: required `file`, optional `order`, `slot`; no subcommands. `src/handlers/commands/swatch.ts:4-9`: "The 4.x index/grid subcommands are replaced by a required `file:` attachment".
- Reviewer candidates: MAN-01 (verdicts in `../evidence/verify-*.md`).

## Fix
- Syntax `/swatch <file> [order] [slot]`; describe the `.chara` read, the 7 slots and the two orders.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`, `45051233`), not merged. Becomes FIXED on merge + the discord-worker deploy it triggers.
