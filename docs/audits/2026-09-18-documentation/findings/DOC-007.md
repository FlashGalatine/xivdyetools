# DOC-007: `/manual` gives `/gradient` the wrong option names and the wrong step range
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:66-67` — `manual.gradient`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `schemas.ts:382-402`: `start_color`, `end_color`, `steps` min 2 max 12 (default 6, `handlers/commands/gradient.ts:38`). The text says `<start> <end>` and "(2-10)".
- Reviewer candidates: MAN-04 (verdicts in `../evidence/verify-*.md`).

## Fix
- Syntax `/gradient <start_color> <end_color> [steps] [color_space]`; "2-12", nine colour spaces.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`), not merged. Becomes FIXED on merge + discord-worker deploy.
