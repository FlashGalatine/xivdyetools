# DOC-014: The preview-image moderation route is documented with a body the handler rejects
**Severity:** MEDIUM · **Kind:** MISSING · **Deploy unit:** presets-api · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/projects/presets-api/endpoints.md:282` — body `{ action }`
- `docs/architecture/api-contracts.md:496-503` — same

## Evidence
- `apps/presets-api/src/handlers/moderation.ts:282-289` 400s unless `preview_image_key` is present and matches `^<presetId>/….webp$`; `:291-295,312,328` return 409 "changed or was already moderated". A client built from the doc always gets a 400.
- Reviewer candidates: PMO-05 (verdicts in `../evidence/verify-*.md`).

## Fix
- Add `preview_image_key` to both examples and list the 400 and 409.

## Status
FIXED 2026-09-18 `b7754ee9` (PR #190).
