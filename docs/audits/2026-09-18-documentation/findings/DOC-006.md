# DOC-006: `/manual` lists 7 of `/harmony`'s 10 types and never mentions colour wheels
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:58-59` — `manual.harmony`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `schemas.ts:28-39` has 10 types (adds Inverted Tetradic, Compound, Shades); `:174-214` adds `wheel` (5 wheels), `companions` (1-3), `matching`. `docs/user-guides/discord-bot/command-reference.md:41` is right.
- Reviewer candidates: MAN-03 (verdicts in `../evidence/verify-*.md`).

## Fix
- Say 10 types and 5 wheels; syntax `/harmony <color> [type] [wheel] [companions]`.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`, `45051233`, review fixes `e94f8fd4`), not merged. Becomes FIXED on merge + the discord-worker deploy it triggers.
