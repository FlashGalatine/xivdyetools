# DOC-011: `/manual` tips: "All commands support autocomplete" is false, and the Facewear tip describes a filter that no longer exists
**Severity:** LOW · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:48` — `manual.autocompleteNote`
- `packages/bot-logic/src/i18n/locales/en.json:105` — `manual.tips.facewearExcluded`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- Dye-name autocomplete exists on 11 of 17 registrations (`autocomplete: true` fields in `schemas.ts`), not on `/swatch`, `/extractor image`, `/dye list|random`, `/about`, `/manual`, `/changelog`, `/stats`.
- `packages/core/src/data/dyes.json` has no Facewear rows since schema v2 — nothing is "excluded"; the outcome (they never appear) still holds.
- Reviewer candidates: verifier facts 15-16 (verdicts in `../evidence/verify-*.md`).

## Fix
- "Dye options support autocomplete"; reword the Facewear tip as "Facewear colours are not dyes and never appear".
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`, `45051233`), not merged. Becomes FIXED on merge + the discord-worker deploy it triggers.
