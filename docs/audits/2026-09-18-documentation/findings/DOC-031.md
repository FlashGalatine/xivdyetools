# DOC-031: presets-api `endpoints.md` banner gives the legacy-itemID cutoff as 5729; the check is 5000
**Severity:** LOW · **Kind:** WRONG · **Deploy unit:** presets-api · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `docs/projects/presets-api/endpoints.md:6`

## Evidence
- `apps/presets-api/src/services/validation-service.ts:217` `id >= 5000`. Only the error message differs (`:221` rejects everything over 254 regardless), and the source comment at `:213` also says 5729.
- Reviewer candidates: PMO-04 (verdicts in `../evidence/verify-*.md`).

## Fix
- Say "legacy itemIDs" without the number, or fix both the doc and the comment.

## Status
FIXED 2026-09-18 `b7754ee9` (PR #190). Doc only; the source comment at `validation-service.ts:213` still says 5729 — deferred, see the report.
