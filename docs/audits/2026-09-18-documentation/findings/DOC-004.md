# DOC-004: `/manual topic:match_image` documents `/match` and `/match_image`, both deleted in 5.0, with four wrong numbers
**Severity:** MEDIUM · **Kind:** PLANNED-VS-SHIPPED · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:437-464` — every `matchImageHelp.*` key. Same line in `ja`, `de`, `fr`, `ko`, `zh`.
- `apps/discord-worker/src/handlers/commands/manual.ts:35-116,386` renders it; `src/handlers/commands/extractor.ts:581` sends `/extractor image` users to it.

## Evidence
- `extractor.ts:10` "Replaces: /match, /match_image". Wrong numbers: "136 FFXIV dyes" (125), "Euclidean distance in RGB" (default ΔE2000), "`colors` option (1-5)" (`schemas.ts:344-350`: 3-10), "8MB" (`apps/image-worker/src/validators.ts:38`: 10 MB).
- The "Match Quality Ratings" field (Perfect/Excellent/Good…) is a vocabulary the 5.0 cards no longer print.
- Reviewer candidates: MAN-02 (verdicts in `../evidence/verify-*.md`).

## Fix
- Rewrite the topic around `/extractor image`; keep the topic id `match_image` so the slash-command shape does not change.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`, `45051233`), not merged. Becomes FIXED on merge + the discord-worker deploy it triggers.
