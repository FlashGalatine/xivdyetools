# DOC-009: `/manual topic:character_file` names 5 of `/swatch`'s 7 colour slots
**Severity:** MEDIUM · **Kind:** WRONG · **Deploy unit:** discord-worker · **Baseline:** `0fec18f4` (served by every production deploy workflow, = `origin/main`), checked 2026-09-18

## Location
- `packages/bot-logic/src/i18n/locales/en.json:594` — `manual5.topics.characterFile.body`. Same line in `ja`, `de`, `fr`, `ko`, `zh`.

## Evidence
- `schemas.ts:888-900` / `handlers/commands/swatch.ts:56-64`: skin, hair, highlights, eyes, lip, facepaint, limbal. Highlights and tattoo/limbal ring are missing, so a user cannot learn `slot:` accepts them.
- Reviewer candidates: MAN-06 (verdicts in `../evidence/verify-*.md`).

## Fix
- Name all seven slots.
- Fixed by the `/manual` update PR (see the report); bot-logic publish + discord-worker deploy follow the merge.

## Status
FIX PROPOSED 2026-09-18 — draft PR #189 (`29e055e9`), not merged. Becomes FIXED on merge + discord-worker deploy.
