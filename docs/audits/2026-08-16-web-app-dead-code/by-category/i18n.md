# i18n — Summary

## Overview
- **Total Findings:** 1 (DEAD-022)
- **Recommended for Removal:** 472 keys × 6 locales
- **Estimated Bytes:** 131,219 (26.2 % of every locale file)

| ID | Scope | Confidence | Recommendation |
|----|-------|------------|----------------|
| DEAD-022 | 472 dead keys — whole `matcher.*` namespace, pre-5.0 preset browser, old gradient-mixer vocabulary, `comparison.chart.*`, `config.*` duplicates, all `tools.*.subtitle`, `preset.<id>.tags.*` … | HIGH | REMOVE from all six locales in one commit |

**Verified live (do not remove):** `tutorial.*` (47 keys, all literal), `tools.<x>.{title,shortName,description}` (via `${tool.translationKey}`), `mixer.model{Rgb,…}` (via `mixer.model${Model}`), `accessibility.*`/`comparison.*` metric stems, `harmony.types.*Desc`, `preset.<id>.{name,description}`, `swatch.*` chara-import strings, `themes.standard*`. Full false-positive list (160) in `evidence/agent-report-i18n.md`.
